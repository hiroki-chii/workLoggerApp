# PulseWork アプリケーション仕様書

本書は、後任のエンジニアや開発者が PulseWork のシステム構成、動作の仕組み、データ構造、開発・ビルド手順について理解し、スムーズに機能追加や保守を行えるようにするための仕様書です。

---

## 1. アプリケーション概要
PulseWork は、ユーザーのPC上の作業状況（アクティブなアプリケーション名やウィンドウタイトル）を自動記録し、分析・可視化を行う Windows 向けの Electron デスクトップアプリケーションです。

### 1.1 主な特徴
- **自動サンプリング**: 10秒ごとにアクティブウィンドウの情報と無操作時間を取得。60秒（1分）以上の無操作を検出した場合はログの記録を自動的に停止。
- **疲労度算出機能**: 60分スライディングウィンドウ方式を採用。稼働率から疲労状態を5段階（`Restored` / `Calm` / `Focused` / `Strained` / `Critical`）で判定。
- **ポモドーロモード**: 15分/25分/50分のポモドーロ・テクニックに対応。作業と休憩のサイクルを管理し、フェーズ切り替え時に通知。
- **ミニウィンドウ（ウィジェット）**: 最前面に表示される小型ウィンドウ。疲労度、アドバイス、ペット、ポモドーロタイマーをリアルタイムで確認可能。
- **活動の記録（タイムテーブル）**: 15分刻みの詳細なヒートマップ。セルクリックで詳細なログ内訳（アプリ/ウィンドウ別集計）を表示。
- **ステータスインジケーター**: 「アプリの記録状態（緑）」と「PCの操作状態（青）」を示す2つのドットを表示。
- **表示名の変更ルール**: 正規表現等によるウィンドウタイトルの置換機能。置換後の名前や専用の色をフロントエンドで動的に適用。
- **システムトレイ対応**: 最小化時やクローズ時にバックグラウンドで動作を継続。

---

## 2. システム構成・技術スタック

本アプリは、**Electron + React + Express + SQLite3** のマルチプロセス構成になっています。

```mermaid
graph TD
    subgraph Frontend (Vite + React)
        A[UI / App.jsx]
    end

    subgraph Electron Main Process
        B[main.js]
        C[Tray / UI Manager]
    end

    subgraph Backend Services
        D[Express API / server.js]
        E[Collector Agent / collector.js]
        F[PowerShell / monitor.ps1]
    end

    subgraph Data Store
        G[(logs.db / SQLite3)]
    end

    A <-->|HTTP / fetch| D
    A <-->|IPC| B
    D <-->|Better-SQLite3| G
    E <-->|Better-SQLite3| G
    E <-->|spawn| F
    B <-->|spawn| D
    B <-->|spawn| E
```

### 2.1 技術スタック
- **Core**: Electron v34.0.0
- **Frontend**: React (Vite), Vanilla CSS, Lucide-React, Chart.js
- **Backend**: Express v5.2.1, Better-SQLite3 v12.8.0 (WAL モード)
- **OS連携**: Windows PowerShell (ウィンドウ取得・アイドル検知)
- **ビルド**: Electron-Builder v24.13.3

---

## 3. データベース設計（SQLite3）
データは `%APPDATA%\workloggerapp\logs.db` に保存されます。

### 3.1 テーブル構成

#### `logs` テーブル (活動ログ)
| カラム名 | 型 | 説明 |
| :--- | :--- | :--- |
| `id` | INTEGER | PRIMARY KEY |
| `appName` | TEXT | アプリ名 |
| `windowTitle` | TEXT | ウィンドウタイトル |
| `timestamp` | DATETIME | UTCタイムスタンプ |

#### `settings` テーブル (設定)
主要なキー:
- `sampling_interval`: 10秒固定。
- `idle_threshold`: 無操作判定しきい値。**60秒（1分）固定**。
- `current_mode`: `tracking` または `pomodoro15/25/50`。
- `pomodoro_status`: `running` または `paused`。
- `mini_window_position`: `"右下"` 等（座標保存も対応）。
- `show_mini_on_close`: クローズ時にミニ画面を表示するか。

#### `window_rules` テーブル (表示名変更ルール)
ウィンドウタイトルを置換し、色を割り当てるためのルールを保持。

---

## 4. ログ収集（Collector）とアイドル判定

### 4.1 収集ロジック
1. 10秒ごとに `monitor.ps1` を実行。
2. `GetLastInputInfo` による無操作時間が **60秒** を超えた場合、記録をスキップ。
3. アイドル状態でなければ `logs` テーブルに保存。
4. PC自体の操作状態（Idle/Active）は `/api/status` を通じてフロントエンドへリアルタイム通知。

---

## 5. 疲労度・ポモドーロ判定アルゴリズム

### 5.1 疲労度判定（トラッキングモード）
直近 **60分間** の稼働状況を対象とした判定。
- **稼働率 (%)** ＝ `有効ログ数 ÷ 期待ログ数(3600秒/11秒) × 100`

| ステータス | 判定基準（アイドル率） | 特徴 |
| :--- | :--- | :--- |
| **`Restored`** | 40% 以上 | 十分な休息あり |
| **`Calm`** | 25% 〜 39% | ゆったり |
| **`Focused`** | 15% 〜 24% | 良いペース |
| **`Strained`** | 10% 〜 14% | 疲れ気味（汗のペット） |
| **`Critical`** | 10% 未満 | 限界（焦るペット＋アラート） |

- **Initializing**: 起動後、最初のログが取得されるまでの待機状態。

### 5.2 ポモドーロモード
- **Workフェーズ**: 集中作業時間（15/25/50分）。
- **Breakフェーズ**: 短期休憩時間（3/5/10分）。
- 残り時間はフロントエンドで1秒刻みのカウントダウン。フェーズ切替時に最前面アラートを表示。

---

## 6. フロントエンド UI 機能

### 6.1 各画面の説明
- **ダッシュボード**: 
  - 稼働状況（アプリ/ウィンドウ数）、円グラフ、疲労度/ポモドーロカード。
  - モード切替、タイマー操作（スタート/停止/リセット）。
- **タイムテーブル**: 
  - 1週間分を15分刻みで表示。セルクリックで詳細内訳（使用割合・合計時間）を表示。
  - 「現在時刻へジャンプ」機能。
- **履歴**: 直近1000件のログリスト。
- **設定**: 表示名変更ルール管理、記録削除、リセット。
- **ヘルプ**: 90分ウィンドウ（表示上）や120秒（表示上）の解説を含むガイド（※内部ロジックと表示に一部差異あり）。

---

## 7. 開発・保守

### 7.1 コマンド
- `npm run dev`: 開発起動（Frontend + Electron + Backend）。
- `npm run build`: EXEビルド。

---

## 8. トラブルシューティング
- **二重通知**: メインとミニ両方開いている場合、ミニ側でアラートを抑制。
- **ポート衝突**: 3001番ポートが使用中の場合はタスクマネージャーから旧プロセスを終了。
