# PulseWork アプリケーション仕様書

本書は、後任のエンジニアや開発者が PulseWork のシステム構成、動作の仕組み、データ構造、開発・ビルド手順について理解し、スムーズに機能追加や保守を行えるようにするための仕様書です。

---

## 1. アプリケーション概要
PulseWork は、ユーザーのPC上の作業状況（アクティブなアプリケーション名やウィンドウタイトル）を自動記録し、分析・可視化を行う Windows 向けの Electron デスクトップアプリケーションです。

### 1.1 主な特徴
- **自動サンプリング**: 10秒ごとにアクティブウィンドウの情報と無操作時間を取得。120秒（2分）以上の無操作を検出した場合はログの記録を自動的に停止。
- **疲労度算出機能**: 作業の開始時刻、稼働時間、アイドル率から疲労状態を5段階（`Restored` / `Calm` / `Focused` / `Strained` / `Critical`）で判定し、アドバイスを表示・Critical時に最初の一回のみ警告アラートを通知。
- **ミニウィンドウ（ウィジェット）**: PC画面上の最前面に小さく表示されるウィンドウで、作業を邪魔せず疲労度やアドバイス、ミニグラフをリアルタイムで確認可能。
- **活動の記録（タイムテーブル）**: 24時間×1週間のタイムテーブル。15分刻みでユーザーが何に時間を割いていたかを色（表示名変更ルールで設定可能）と名前で直感的に可視化。
- **表示名の変更ルール**: 元のウィンドウタイトルをデータベースにそのまま保持しつつ、正規表現/部分一致/前方一致/完全一致で指定したキーワードを任意の分かりやすい別名や色に置き換えて表示するフロントエンド完結型の機能。
- **CSV エクスポート**: 指定期間内のログ（JST）を CSV 形式でダウンロード。
- **システムトレイ（タスクトレイ）対応**: アプリのクローズボタンを押した際にもバックグラウンドで動き続け、いつでも呼び出しが可能。

---

## 2. システム構成・技術スタック

本アプリは、**Electron + React + Express + SQLite3** のマルチプロセス構成になっています。

```mermaid
graph TD
    subgraph Frontend (Vite + React)
        A[UI / App.jsx]
    end

    subgraph Electron Main Process
        B[main.js]
        C[Tray / UI Manager]
    end

    subgraph Backend Services
        D[Express API / server.js]
        E[Collector Agent / collector.js]
        F[PowerShell / monitor.ps1]
    end

    subgraph Data Store
        G[(logs.db / SQLite3)]
    end

    A <-->|HTTP / fetch| D
    A <-->|IPC| B
    D <-->|Better-SQLite3| G
    E <-->|Better-SQLite3| G
    E <-->|spawn| F
    B <-->|spawn| D
    B <-->|spawn| E
```

### 2.1 技術スタック
- **Core (デスクトッププラットフォーム)**:
  - **Electron**: v34.0.0
- **Frontend (フロントエンド)**:
  - **React** (Vite + JavaScript)
  - **CSS**: Vanilla CSS
  - **アイコン**: Lucide-React
- **Backend (バックエンド / 収集ロジック)**:
  - **Express**: v5.2.1
  - **Better-SQLite3**: v12.8.0（WAL モード有効）
  - **Windows PowerShell**: 最前面ウィンドウおよびアイドル時間のサンプリングに利用
- **ビルドツール**:
  - **Electron-Builder**: v24.13.3

---

## 3. データベース設計（SQLite3）
データは、システムの `APPDATA/workloggerapp/logs.db`（Windowsの場合は `%APPDATA%\workloggerapp\logs.db`）に保存されます。

### 3.1 テーブル構成

#### `logs` テーブル (活動ログ)
ユーザーがアクティブにPCを操作していると判定された際に、10秒ごとにログレコードが追加されます。
| カラム名 | 型 | 制約 | 説明 |
| :--- | :--- | :--- | :--- |
| `id` | INTEGER | PRIMARY KEY AUTOINCREMENT | ログID |
| `appName` | TEXT | - | 実行中のアプリケーションプロセス名（例: `chrome`, `code`） |
| `windowTitle` | TEXT | - | アクティブウィンドウのタイトル |
| `timestamp` | DATETIME | DEFAULT CURRENT_TIMESTAMP | ログ記録日時（UTC） |

#### `settings` テーブル (アプリケーション設定)
アプリの設定をキー・バリュー形式で保存します。
| カラム名 | 型 | 制約 | 説明 |
| :--- | :--- | :--- | :--- |
| `key` | TEXT | PRIMARY KEY | 設定のキー |
| `value` | TEXT | - | 設定の値 |

##### 保存される主要な設定キー
- `sampling_interval`: サンプリング間隔。**10秒固定**。
- `idle_threshold`: 無操作時間のしきい値（秒）。**120秒（2分）固定**（これを超えると記録をスキップ）。
- `default_activity_color`: 標準の色。**`#6366f1` 固定**。
- `show_mini_on_close`: メインウィンドウを閉じた際にミニ画面を表示するか。デフォルト `"true"`。
- `mini_window_position`: ミニ画面の起動時のデフォルト位置。`"左上"`, `"右上"`, `"左下"`, `"右下"`。
- `show_pet_in_mini`: ミニ画面にペットを表示するかどうか。デフォルト `"true"`。

#### `window_rules` テーブル (表示名変更ルール)
特定のアプリケーション名やウィンドウタイトルを指定の表記・色に変換して表示するためのルール。
| カラム名 | 型 | 制約 | 説明 |
| :--- | :--- | :--- | :--- |
| `id` | INTEGER | PRIMARY KEY AUTOINCREMENT | ルールID |
| `keyword` | TEXT | NOT NULL | 判定用のキーワード |
| `replace_with` | TEXT | NOT NULL | 置換後の名前 |
| `match_type` | TEXT | NOT NULL | `部分一致`, `前方一致`, `完全一致` のいずれか |
| `color` | TEXT | - | タイムテーブルやリストに適用する 16 進カラーコード |

---

## 4. ログ収集（Collector）とアイドル判定

ログの収集は `backend/collector.js` がバックグラウンドプロセスとして起動・管理しています。

### 4.1 収集ロジックのフロー
1. **PowerShell 呼び出し**: `backend/monitor.ps1` を 10秒に一度呼び出します。
2. **データの取得**:
   - `user32.dll` の `GetForegroundWindow`, `GetWindowText`, `GetWindowThreadProcessId` を使い、現在フォーカスされているウィンドウタイトルとアプリケーションプロセス名を抽出。
   - `GetLastInputInfo` からPCが無操作である時間（秒）を算出。
   - JSON 形式でサーバーへ返却。
3. **アイドル（無操作）判定**:
   - `idleSeconds`（無操作時間）が **120秒（2分）** を超えている場合、**ログの記録はスキップ** されます。
   - これにより、席を外している時間の無駄なデータ蓄積や、CPU・ディスクへの負荷を抑えています。
4. **保存**: アイドル状態でなければ `logs` テーブルに UTC タイムスタンプでレコードが保存されます。

## 5. 疲労度算出アルゴリズム

`GET /api/fatigue` でリアルタイムにユーザーの疲労度とアドバイス、およびステータスを算出します。

### 5.1 ステータスの決定基準
過集中を防止し、休憩をより分散させやすくするため、**60分スライディングウィンドウ方式**（直近60分間の活動状況のみを対象とした判定）を採用しています。

- **判定基準期間**: `max(本日の初回ログ時刻, 現在時刻 - 60分)` から現在時刻までの経過時間。
- **猶予期間（30分）**: ウィンドウ内の経過時間が `1800秒`（30分）未満の間は、どんな場合でも無条件にステータスは **`Calculating`** と判定されます。
- **データなしの場合**: 本日のログが 1 件もない場合はアイドル率 0% で **`Restored`** 判定となります。

#### 5段階のステータス判定（ウィンドウ内アイドル率ベース）
**ウィンドウ内アイドル率 (%)** ＝ `(想定されるウィンドウ内期待ログ数 - 実際の有効ログ数) ÷ 想定されるウィンドウ内期待ログ数 × 100`

| アイドル率 / 条件 | ステータス | 特徴・アドバイス |
| :--- | :--- | :--- |
| ウィンドウ内の経過時間 30分未満 | **`Calculating`** | 稼働データの集計を開始しました。しばらくお待ちください。 |
| `40%` 以上 | **`Restored`** | 今日も一日頑張りましょう！！（十分な休息が取れている状態） |
| `25%` 〜 `39%` | **`Calm`** | ゆったりと作業を進めています。マイペースにいきましょう！ |
| `15%` 〜 `24%` | **`Focused`** | 良いペースです！この調子でいきましょう！ |
| `10%` 〜 `14%` | **`Strained`** | 集中していますが、少し疲れが出やすい状態です。適度に水分補給を。 |
| `10%` 未満 | **`Critical`** | 完全に集中しすぎています！至急リフレッシュを。 |

### 5.2 Critical判定時のアラート機能
ステータスが **`Critical`** に達した場合、過度なアラートを防ぐため、**切り替わった最初の一回のみ** 以下の 2 つの通知が発火します。
1. **OS標準通知（HTML5 Notification API）**: 「長時間の作業お疲れ様です。そろそろ休憩を取りませんか？」などのプッシュ通知。
2. **ポップアップダイアログ**: Electron のネイティブのダイアログボックス（`dialog.showMessageBox`）が前面にポップアップし、確実に休憩を促します。


---

## 6. フロントエンド UI 機能解説

React アプリは SPA で開発されており、以下の 5 つの主要画面で構成されています。

### 6.1 各画面・タブの説明
- **概要（ダッシュボード）**:
  - 本日の作業状況、使用したアプリ数、使用したウィンドウ数をリアルタイムで表示。
  - 円グラフと、使用率の高い順上位 6件の進捗バー付きリストを表示（6件目以降は自動的に「その他」として集計・表示崩れを防ぐ）。
  - **疲労度カード**: ユーザーのステータスと、疲労状態に連動したアドバイスを表示。
- **活動の記録（タイムテーブル）**:
  - 1週間分の活動を 15分刻み（行: 6:00〜翌5:45、列: 曜日別）で並べたヒートマップ（色付き枠）形式。
  - セルにマウスを乗せると、その時間枠のアプリ名、ウィンドウ名、作業占有率をポップアップで表示。
  - セルをクリックすると、「内訳モーダル」が開き、その 15分間に含まれるすべての作業ログの詳細を確認可能。
- **履歴**:
  - サンプリングされたログの時系列リスト（最大 1000件）。
- **設定**:
  - 「表示名の変更ルール」の登録・変更・削除。
  - 「ミニ画面」のクローズ連動自動起動、および起動時の位置設定（左上・右上・左下・右下）。
  - 「記録の削除とリセット」: 本日のログのリセット、または全ログの完全削除。
- **ヘルプ**:
  - 操作手順や、データ保存場所（DB・エクスポート）に関するガイドライン。

---

## 7. 開発・保守手順とコマンド

### 7.1 プロジェクトディレクトリ構成
```text
workLoggerApp/
├─ assets/             # アイコン等の静的リソース
├─ backend/            # Expressサーバー、PowerShell、ログ収集エージェント
│  ├─ collector.js     # ログ収集エージェント
│  ├─ monitor.ps1      # ウィンドウ取得PowerShellスクリプト
│  └─ server.js         # APIサーバー
├─ electron-core/      # Electronメインプロセス
│  └─ main.js          # ウィンドウ管理、システムトレイ、IPC
├─ frontend/           # Vite + React フロントエンド
│  ├─ src/
│  │  ├─ App.jsx       # メインコンポーネント (SPA)
│  │  ├─ App.css       # スタイル定義
│  │  └─ ThemeProvider.jsx # テーマ管理 (Light/Dark/System)
│  ├─ index.html
│  └─ vite.config.ts
├─ history.md          # 修正・改修履歴（※1ラリーごとに日時含め逐一記録）
├─ package.json        # ルートの package.json (全体起動やビルドのタスク登録)
└─ specification.md    # 本書
```

### 7.2 コマンド一覧
ルートフォルダで実行可能な主要スクリプトです。

#### 開発サーバーの起動 (フロントエンドとElectronの同時起動)
```bash
npm run dev
```
※フロントエンドサーバー（`http://localhost:5173`）が起動した後、Electronが起動し、バックグラウンドでExpressサーバー（`http://localhost:3001`）とCollector（ログ収集エージェント）が同時に自動実行されます。

#### プロダクションビルド（EXEファイルの生成）
```bash
npm run build
```
※フロントエンドが `frontend/dist` にビルドされ、その後 `electron-builder` を用いてインストーラー（NSIS / Portable）が `dist-pulse` ディレクトリに出力されます。

---

## 8. トラブルシューティング
- **ポート衝突 (`3001` が使用中)**
  - 過去に起動した PulseWork や Node.exe プロセスがシステムトレイに残っている可能性があります。タスクマネージャー、またはトレイアイコンからアプリを完全に終了（「終了」またはフロントエンド下部の「アプリを完全に閉じる」ボタン）させてください。
- **表示名変更ルールや色がタイムテーブルに反映されない**
  - ルールの `match_type`（キーワードを含む / キーワードから始まる / キーワードと同じ）が正しく選択されているか確認してください。文字の大文字小文字やスペースも含めて比較されるため、登録時の表記を正確に合わせる必要があります。
