# 軽量化調査・リファクタリング計画

調査・実装日: 2026-09-20。保存済みログは変更していない。

## 実装結果

優先度1と配布軽量化を実装した。最終成果物は `dist-refactor-final` に出力した。

- 非表示画面ではAPI取得とアニメーションを停止。ミニ画面は設定、疲労度、無操作状態だけを取得する。
- ダッシュボード、履歴、タイムテーブル、設定の各画面で必要なAPIだけを取得し、設定等は変更時までキャッシュする。
- 疲労・ポモドーロ通知をサーバープロセスへ移し、画面非表示時も通知し、複数画面からの重複通知を防ぐ。
- PowerShellをサンプルごとに起動する方式から、1プロセスを再利用する方式へ変更した。異常時のタイムアウト、バックオフ、終了時の回収を追加した。
- `logs.timestamp`索引と範囲条件を導入。ログ一覧の並び替えも索引を利用できる列に修正した。
- 省電力表示を既定で有効化し、ぼかし効果を抑えた。ペットはミニ画面だけに表示し、非表示時は描画を一時停止する。
- ElectronのGPU標準動作へ戻した。互換性問題がある環境では `WORKLOGGER_DISABLE_GPU=1` で従来のGPU無効化へ戻せる。
- 多重起動を防止し、スリープ中は監視を止め、復帰時に再開する。
- 日本語・英語以外のElectron言語ファイル、SQLiteのビルド用ソース、未使用依存を配布対象から除外した。ASARと通常圧縮を有効化した。

| 対象 | 変更前 | 変更後 | 削減率 |
|---|---:|---:|---:|
| Setup EXE | 83.62 MiB | 73.31 MiB | 12.3% |
| Portable EXE | 82.71 MiB | 73.06 MiB | 11.7% |
| win-unpacked | 287.50 MiB | 237.83 MiB | 17.3% |

通常のメイン画面＋ミニ画面を同時に使う場合、定期HTTP取得は従来の最大18本/10秒から、ダッシュボード表示中は定常6本/10秒へ減る。メイン画面を隠してミニ画面だけにすると定常2本/10秒、トレイだけなら0本になる。

自動テスト10件、フロントエンドビルド、Windows配布ビルド、最終ASARからのAPI・SQLite起動確認に合格した。影響PC上での長時間CPU・消費電力測定は、実運用後の確認項目として残る。

## 結論

動作の軽量化は、①非表示画面とミニ画面による重複処理、②描画の継続、③PowerShellの定期起動、④蓄積ログの全件走査を優先する。容量はElectron本体が大部分を占めるため、まず不要な言語・ビルド用ファイルを配布物から除外する。

ファンとバッテリー消費への寄与率は未確定。GPU無効化、アニメーション、DB処理を個別に切り替える比較測定が必要。

## 確認結果

### 実行中の負荷

- 起動中の「ゆとリズム」7プロセスを20.19秒観測。CPU時間の増分は合計約20.66秒、論理CPU 1コア換算で約102%。タスクマネージャーのマシン全体を100%とする値とは異なる。
- 最大の1プロセスは1コア換算約68%。プロセスの役割取得はOSのアクセス拒否により未確認。rendererやGPUプロセスが原因とは断定できない。
- 観測終了時のワーキングセット合計は約605.6 MiB。共有ページの重複を含むため、専有メモリ量ではない。
- ウィンドウ表示状態・操作状況を固定していない短時間観測。PowerShellの短命プロセスも集計対象外。問題が起きる別PCでの再現、消費電力、ファン回転数は未測定。

### 容量（MiB = 1,048,576 bytes）

| 対象 | 実測 | 判断 |
|---|---:|---|
| 既存Setup EXE | 83.62 MiB | インストーラー単体 |
| 既存portable EXE | 82.71 MiB | 別の配布形式。両方を配る必要はない |
| win-unpacked全体 | 287.50 MiB | 展開後の基準。ファイル長の合計 |
| 上記の実行ファイル | 181.78 MiB | Electronランタイムの大きな固定費 |
| 上記のlocales 55ファイル | 41.26 MiB | ja/en-USのみなら約1.17 MiB。約40.09 MiBが削減候補 |
| resources/app全体 | 15.04 MiB | アプリ固有部分は比較的小さい |
| 同梱better-sqlite3 | 11.58 MiB | sqlite3.cだけで8.96 MiB。実行に必要な.nodeは保持 |
| frontend/dist | 0.43 MiB | 配布容量の主因ではない |
| 開発用node_modules（ルート／frontend） | 488.55 / 84.35 MiB | 開発環境の容量。これら全量が配布されているわけではない |

ローカルのlogs.dbは約6.73 MiB、WALは約4.26 MiB。サイズだけ確認し、利用履歴の内容は読み取っていない。これらは配布物とは別のユーザーデータ。

既存成果物と現在の設定は区別する。現在のpackage.jsonにはcompression: storeがあるが、既存builder-effective-config.yamlにはこの項目がない。既存EXEを「無圧縮」とは判断しない。backendの3ファイルは配布物と現行ソースのハッシュ一致を確認。main.jsは初回読み取り後の再アクセスが拒否され、ハッシュ比較は未実施。

## 問題箇所と対応方針

### 優先度1: 隠した画面でも集計を繰り返す

根拠: [App.jsx:342](C:/Users/hirok/dev/workLoggerApp/frontend/src/App.jsx:342)、[App.jsx:610](C:/Users/hirok/dev/workLoggerApp/frontend/src/App.jsx:610)、[main.js](C:/Users/hirok/dev/workLoggerApp/electron-core/main.js)。

- 10秒ごとに8本のデータAPI、status API、記録状態のIPCを呼ぶ。通常1画面あたり54 HTTPリクエスト/分。
- メイン画面はbackgroundThrottling: falseで、閉じてもhideするだけ。ミニ画面も同じAppを読み、表示分岐より前の共通取得処理を実行する。両方が動く場合は通常108 HTTPリクエスト/分に加え、両画面で変換・集計が発生する。
- タブに関係なく統計2種、最大1,000件のログ、ヒートマップ、全履歴由来のタイトル候補等を取得する。
- リクエスト重複防止・キャンセルがなく、遅い環境では定期取得と同期イベントによる取得が重なる余地がある。

対応: メイン画面・ミニ画面を分離し、表示中の画面に必要なデータだけ取得する。設定とルールは初回・変更時に更新。非表示時の集計取得を止め、再表示時に更新する。取得中の再実行を抑止し、古い応答の反映を防ぐ。

重要な順序: 現状、疲労通知とポモドーロ通知はメイン画面のReact処理が担当している。先に通知・タイマー状態の管理を常駐側へ移し、その後に画面の処理を止める。単純なタイマー停止では通知が失われる。

### 優先度1: GPU無効化と常時アニメーション

根拠: [main.js](C:/Users/hirok/dev/workLoggerApp/electron-core/main.js)、[App.css:1008](C:/Users/hirok/dev/workLoggerApp/frontend/src/App.css:1008)、[App.jsx:1747](C:/Users/hirok/dev/workLoggerApp/frontend/src/App.jsx:1747)。

- 起動時にdisable-gpuを常に指定。コメントによるとWindows更新後のクラッシュへの暫定対策。
- ペットの無限アニメーションが複数あり、透明なミニ画面にはblurもある。CPU描画時の継続負荷を疑うが、寄与率は未測定。

対応: 非表示時にアニメーションを停止し、省電力設定でペットを静止表示できるようにする。GPU有効/無効、アニメーション有/無、透明・blur有/無を比較する。GPUの一律無効化は互換モードへ移す案とし、問題のあったPCでクラッシュしないことを確認してから変更する。backgroundThrottlingの既定動作復帰は通知移管後に実施する。

### 優先度1: 10秒ごとのPowerShell起動とC#コンパイル

根拠: [collector.js:57](C:/Users/hirok/dev/workLoggerApp/backend/collector.js:57)、[monitor.ps1:1](C:/Users/hirok/dev/workLoggerApp/backend/monitor.ps1:1)。

- サンプルごとにpowershellを新規起動し、Add-TypeでWin32呼び出し用C#をコンパイルする。既定間隔では最大約360回/時。
- 無操作で記録をスキップするときも同じ処理を実行。検出失敗時にはGet-Processで全プロセスを列挙する。
- 処理が設定間隔を超えると次の待機が0になり、重い環境では連続起動に近づく。

対応: 第一段階ではPowerShellを単一の常駐プロセスにし、Add-Typeを初回だけ実行、JSONを行単位で送る。-NoProfile、異常時のバックオフ、タイムアウト、終了時の子プロセス回収を組み込む。idle判定はElectron powerMonitor利用を検討。常駐方式でも負荷が残る場合に小型のWin32ヘルパーを検討する。

記録精度を守るため、初期改善では10秒間隔を維持する。現状は疲労計算が11秒固定、画面は設定値を使用しており不一致がある。間隔を可変化する前に時間の計算仕様をそろえる。無操作・ロック・スリープからの復帰も検証する。

### 優先度1: 蓄積ログ全体への繰り返し検索

根拠: [server.js:45](C:/Users/hirok/dev/workLoggerApp/backend/server.js:45)、[server.js:91](C:/Users/hirok/dev/workLoggerApp/backend/server.js:91)、[server.js:175](C:/Users/hirok/dev/workLoggerApp/backend/server.js:175)、[server.js:373](C:/Users/hirok/dev/workLoggerApp/backend/server.js:373)。

- スキーマにtimestamp索引がない。期間条件にdate(timestamp, 'localtime')等を適用している。
- タイトル候補は全履歴をGROUP BYし、MAX(timestamp)で並べる。これも各画面で10秒ごとに繰り返す。
- 同期DB処理がAPIプロセスを占有し、データが増えるほど他の応答も遅延する。

確認: Node 24.14.1内蔵SQLiteのメモリDBに同じlogsスキーマを作り、実際のSQLでEXPLAIN QUERY PLANを実行。統計・ログ一覧・疲労クエリはSCAN logs。timestamp索引を追加しても統計とログ一覧はSCANのまま。統計をUTCの開始以上・終了未満の範囲検索へ変更すると索引SEARCHになった。本番同梱SQLite・実データでの速度測定は未実施。

対応: timestamp索引とUTC範囲検索をセットで導入する。ログ一覧は変換後のtimestamp別名によるORDER BYをlogs.timestampへ修正し、表示用変換と分離する。タイトル候補は設定画面で必要なときのみ取得し、必要なら専用キャッシュを設ける。ヒートマップ・疲労度の重複計算を共有する。ローカル日付、午前4時の日替わり、期間終端の既存仕様を保持する。

### 優先度2: 配布ファイルの絞り込み

根拠: [package.json](C:/Users/hirok/dev/workLoggerApp/package.json)、既存dist-pulseの実測。

1. 対応言語の方針を確認し、日本語・英語でよければelectronLanguagesをja/en-USに限定する。削減候補約40.09 MiB。
2. better-sqlite3の同梱Cソース等をパッケージ段階で除外する。native rebuildに使う開発環境のファイルは保持。sqlite3.cだけで8.96 MiB削減候補。
3. compressionをnormalで比較する。インストーラー圧縮は展開後の容量や常駐CPUの削減とは別。
4. ASAR有効化を検討する。ASARは無圧縮のアーカイブなので大幅な圧縮効果は期待しない。外部PowerShellスクリプトはextraResources等に配置し、.nodeの展開と起動パスを検証する。
5. ソース上で使用を確認できなかった@google/generative-aiを依存から除く候補にする。ただし既存配布物で大きな容量を占めておらず、効果は限定的。
6. Setupとportableは用途別に提供する。開発出力フォルダー全体の共有を避ける。

言語限定とsqlite3.c除外だけなら、展開後で約49 MiB、約17%の削減余地がある。最終容量は再ビルドで確定する。ライセンスやランタイムDLLを無差別に削除しない。数十MiB規模の展開サイズが必須なら基盤変更が必要になる可能性があるが、今回は移行を第一案にしない。

### 優先度3: 補助的な整理

- ミニ画面のmoveイベントごとのDBオープン・2回書き込みを、移動完了またはdebounce後の1トランザクションへ変更する。
- App全体のポモドーロ毎秒更新を小さい表示コンポーネントに閉じ込め、時刻差から残り時間を算出する。
- ヒートマップの各セルでの配列findをMap参照へ変更する。グラフやログ表も必要な入力が変わった場合だけ更新する。
- 通常運用の詳細ログ出力を抑える。
- サーバーとcollectorのプロセス統合は、上記改善後もメモリ負荷が大きい場合に検討。重い同期SQLをElectronのメインスレッドへ直接移してUIを止めない。
- 履歴の自動削除は今回導入しない。将来、保存期間・集約保存を設ける場合はユーザーの履歴保持方針とCSV出力要件を確認する。

## 実装順序と検証

| 段階 | 変更 | 完了条件 |
|---|---|---|
| 0 | 再現条件と基準値の記録 | リリース版でメイン表示、ミニのみ、トレイのみ、無操作、記録停止を各5分以上測定。プロセス別CPU・GPU・専有メモリ・API回数を記録 |
| 1 | 常駐側への通知移管、画面別取得、非表示停止 | 非表示画面の重い集計APIが0回。ミニ画面から統計・ログ・ヒートマップAPIを呼ばない。通知は非表示でも1回だけ届く |
| 2 | PowerShell常駐化、描画の省電力対応 | 周期的プロセス起動を解消。停止・終了後の監視子プロセス残存なし。GPU互換性と記録精度を維持 |
| 3 | SQL範囲検索・索引・キャッシュ | 同梱SQLiteで索引利用を確認。1万・10万・100万件の合成ログで応答時間を比較し、対象期間外の履歴増加による劣化を抑える |
| 4 | 配布対象・圧縮・ASAR調整 | クリーン環境でインストール、起動、記録、CSV出力、更新後のDB継承を確認。各配布形式と展開後の容量を報告 |

性能の暫定目標は、問題を再現できた同一PC・同一状態でCPU時間を50%以上削減すること。達成保証ではなく比較の合格目安とする。トレイのみでは描画を停止し、残る負荷を記録・通知に必要な処理へ限定する。バッテリーは画面輝度・電源モード・他アプリを固定した30〜60分の比較で評価する。

機能回帰: 記録開始/停止、無操作判定、画面切り替え、疲労通知、ポモドーロの一時停止/再開/フェーズ移行、スリープ復帰、午前4時境界、週表示、タイトル置換、CSV出力、既存DBの移行を確認する。テスト用DBを使い、実利用データに対する削除テストは行わない。

今回は実装前の調査なので、ビルド・アプリの変更後テストは行っていない。調査用SQLはメモリ内のみで実行した。

## 参照

- [Electron BrowserWindow: visibilityとbackgroundThrottling](https://www.electronjs.org/docs/latest/api/browser-window)
- [Electron powerMonitor](https://www.electronjs.org/docs/latest/api/power-monitor)
- [ASAR形式の説明](https://github.com/electron/asar/blob/main/README.md)
- electron-builderの設定は、ローカル導入済みバージョンの型定義も参照。Webの最新版だけを基準に設定を変更しない。
