# 作業履歴

## 2026-05-07
### モード切り替え時の疲労度警告機能の追加
- **メインプロセス（main.js）**: 確認ダイアログを表示するための `alert:confirm` IPCハンドラーを追加。
- **フロントエンド（App.jsx）**: `handleModeChange` を修正し、トラッキングモードからポモドーロモードへ切り替える際に、疲労状態が `Critical` であれば警告アラート（確認ダイアログ）を表示するように実装。
- **効果**: ユーザーが極度の疲労状態でそのまま作業を継続（ポモドーロ開始）しようとした際に、適切な休憩を促すことができるように改善。

## 2026-04-30
### テーマ切り替え機能の追加
- `ThemeProvider`の実装: ダークモード、ライトモード、システム設定の同期に対応。
- `App.css`の修正: CSS変数とクラス（`.light`, `.dark`）によるスタイリング。
- `App.jsx`の修正: サイドバーにテーマ切り替えUIを追加。
- グラフ（Chart.js）の配色をテーマに合わせて動的に変更するように調整。
- localStorageによる設定の永続化を実装。
- ルートの `package.json` に `npm run dev` コマンドを追加（フロントエンドとElectronを同時に起動）。
- アプリ起動時に「記録を開始しますか？」という確認ダイアログが表示されるように修正。
- 期間指定によるログと統計のフィルタリング機能を実装（バックエンド・フロントエンド両対応）。
- ダッシュボードにカレンダー形式の期間選択UIを追加。
- 統計データの表示形式切り替え機能を実装（円グラフ、棒グラフ、進捗バー付きリスト）。
- インポート不足（Cpu, Timer等）によるレンダリングエラー（真っ白な画面）を修正。
- 期間選択で開始日が終了日を超えない（またはその逆）ようにバリデーションを追加。
- 集計単位（アプリ名 / ウィンドウタイトル）を切り替えて統計を表示できる機能を追加。
- 24時間×1週間のタイムテーブル（ヒートマップ）表示機能を実装。
- タイムテーブルを15分刻みに細分化し、日付表示とヘッダー/左列の固定表示に対応。
- タイムテーブルを独立したタブ（サイドバーからアクセス）に移動し、大画面での分析に対応。
- タイムテーブルの各時間枠（15分）にホバーした際、アプリ名とウィンドウ名の両方、および占有率を表示する機能を追加。
- アプリ全体の時刻表示（タイムテーブル、履歴、CSV出力）を日本時間（JST）に完全対応。タイムスタンプの保存をUTCに変更し、取得時にlocaltimeへ変換するように統一。
- 設定タブに「すべてのログを削除」ボタンを追加。バックエンドに削除用エンドポイント（/api/logs/clear）を実装。
- アイドリング時間（マウス・キーボード操作なし）を「Idle」として記録するかどうかを選択できる機能を追加。判定しきい値（秒）もカスタマイズ可能に。
- アイドリング判定しきい値の最低値を10秒、初期値を300秒に変更し、デフォルトで記録オンに設定。また、サンプリング間隔より短い値を設定できないようにバリデーションを追加。
- サイドバーの折りたたみ機能（Collapse/Expand）を実装。サイドバーを固定位置にし、コンテンツのみをスクロールするようにレイアウトを調整。
- タイムテーブルの日付行（ヘッダー）の固定表示を改善。ページ全体のスクロールを廃止し、タブごとに独立したスクロール領域を持つようにレイアウトを刷新。
- タイムテーブルの日付表示を「yy/mm/dd (曜)」形式に変更。
- 土曜日（青）と日曜日（赤）の列を色分けし、視覚的に曜日を判別しやすく改善。
- 棒グラフ表示を廃止し、円グラフとリスト表示に絞ってUIを整理。
- 集計単位に応じてカードのタイトル（アプリ使用分布 / ウィンドウ使用分布）が動的に変わるように改善。
- バックエンドのクエリ実行ログを追加し、集計の信頼性を向上。
- バックエンドの更新が反映されない場合に統計が `undefined` になる問題を、フロントエンド側でのフォールバック処理で修正。

### タイムテーブルの現在時刻ジャンプ機能追加
- タイムテーブル分析画面に「現在時刻へジャンプ」ボタンを追加。
- タイムテーブルの各15分枠セルにIDを付与し、ボタンクリックでスムーズにスクロールする機能を実装。
- ジャンプした際に該当のセルを一時的にハイライト（枠線表示）する視覚効果を追加。
- 表示期間外の場合にアラートを表示するバリデーションを実装。
- 変更内容をGitHubにプッシュ（`main` ブランチ）。

### 設定の上限調整
- 「アイドル判定しきい値」の上限を1800秒から600秒に変更。
- フロントエンドのバリデーションに上限チェックを追加し、不正な値が保存されないように改善。

### ウィンドウタイトルのエイリアス（別名）機能追加
- データベースに `keyword_aliases` テーブルを追加し、`logs` テーブルに `alias` カラムを追加。
- エイリアス管理用のAPIエンドポイント（取得・追加・削除）をバックエンドに実装。
- ログ収集（`collector.js`）時に、ウィンドウタイトルにキーワードが含まれる場合、エイリアスを自動付与して記録するロジックを実装。
- 履歴表示、CSVエクスポート、統計（ウィンドウ別）において、エイリアスが設定されている場合は優先的に表示・集計されるように修正。
- 設定画面にエイリアス（キーワードと別名のペア）を管理するUIを追加。
- 【追加】キーワード入力時に、過去のウィンドウタイトルをプルダウン（`datalist`）から選択できる機能を実装。
- 【追加】エイリアス追加時に確認ダイアログを表示し、過去のログにもエイリアスを遡って適用できる機能を実装。
- 【追加】登録済みのエイリアスをインラインで編集できる機能（PUT /api/aliases/:id）を実装。編集時にも過去のログへ反映するか選択可能。
- 【追加】エイリアスの照合条件を詳細に設定できる機能を追加。「部分一致（を含む）」「前方一致（で始まる）」「完全一致」の選択機能と、「大文字・小文字を区別する」チェックボックスを実装。
- 【修正】キーワード入力のサジェスト（プルダウン）候補に「アイドル状態」も含めるように変更し、「アイドル状態」自体を別名（例：離席中）に変換しやすくしました。
- 【修正】履歴画面およびタイムテーブル分析のツールチップにおいて、エイリアスが適用されている場合でも元のウィンドウタイトルをカッコ付きで併記（例：`エイリアス名 (元のタイトル)`）するようにしました。

### エイリアスの色設定およびデフォルトカラー設定機能追加
- データベースの `keyword_aliases` テーブルに `color` カラムを追加。
- `settings` テーブルに `default_activity_color` を追加。
- エイリアス作成・編集時に色を選択できるUIを追加し、エイリアス未設定時のデフォルト色も設定画面から変更可能に。
- タイムテーブル（ヒートマップ）や履歴画面において、設定された色（またはデフォルト色）が反映されるように修正。

### 起動時の記録開始確認の表示タイミング改善
- アプリ起動時の「記録の開始確認」ダイアログが、UIのレンダリングが完了（データの初期読み込みが完了）してから表示されるように修正。
- `main.js`（メインプロセス）の起動時イベントからダイアログ表示を削除し、フロントエンド（`App.jsx`）からIPC経由で呼び出す方式に変更。
- レンダリング完了後、500msの遅延を設けてダイアログを表示することで、アプリ画面が背後に表示された状態で確認を行えるように改善。

### 作業ログ
- 2026-04-30 14:15: `main.js` から起動時の `showMessageBoxSync` を削除。
- 2026-04-30 14:15: `main.js` に `recording:confirm-start` IPCハンドラを追加。
- 2026-04-30 14:15: `App.jsx` に `useEffect` を追加し、初期ロード完了後に確認ダイアログを呼び出すロジックを実装。
- 2026-04-30 14:15: 記録開始時のデータリフレッシュ処理を追加。
- 2026-04-30 14:20: アプリ使用分布のリスト表示において、パーセンテージの横に実際の稼働分数（サンプリング間隔に基づく計算値）を表示するように修正。
- 2026-04-30 14:35: ヘルプ・ガイド画面（使用方法や注意点）を実装。サイドバーに新しいタブとして追加。
- 2026-04-30 14:40: タイムテーブルのヒートマップ機能（サンプリング密度による色の濃淡）を廃止。作業内容を純粋に色だけで判別する形式に変更。
- 2026-04-30 14:45: 「無操作」という表記をすべて「アイドル状態」に変更。UI上のラベル、ヘルプ、コレクターでの記録用文字列を一律置換。
- 2026-04-30 14:50: エイリアス設定のキーワード候補（サジェスト）から「アイドル状態」と「無操作」を除外するようにサーバー側を修正。
- 2026-04-30 15:05: タイムテーブル分析において、「アイドル状態」および記録停止中の時間を非表示（背景色なし）にするように修正。
- 2026-04-30 15:10: ヘルプ画面にタイムテーブルのタスク判定条件（15分単位で最も多い作業を表示）の記述を追加。

### 実行ファイルのビルド
- 2026-04-30 15:23: `PulseWork` の実行ファイル（EXE）の再ビルドを実施。
- `npm run build` を実行し、`dist-pulse` フォルダ内の `PulseWork 1.0.0.exe` を更新。
- 2026-04-30 15:56: ヘルプ画面のデータ保存先情報を詳細化。メインDB（logs.db）に加え、設定・キャッシュ（Local Storage等）、エクスポートデータの保存場所について網羅的に記載。
- 2026-04-30 18:40: タイムテーブルのタスク（セル）にホバーした際のエフェクトを控えめに変更。スケールを 1.2 から 1.1 に、シャドウを 15px から 8px に縮小。
- 2026-04-30 18:50: タイムテーブルのホバー視認性を向上。隣接セルと馴染まないよう、影を黒系に変更、境界線の強調（var(--text-main)）、明るさ補正（brightness(1.2)）を追加。
- 2026-04-30 19:00: ライトモードの背景色を目に優しいオフホワイト（#f5f5f7）に変更。タイムテーブルの固定ヘッダー等の背景色をハードコードから変数（--sticky-bg）に変更し、テーマ対応を強化。
- 2026-04-30 19:10: タイムテーブルの視認性を改善。固定ヘッダーのコントラストを強化（背景との色差を拡大）。土日の列の背景色を濃く（透明度 0.03→0.1）し、ヘッダー部分にも曜日色を適用。
- 2026-04-30 19:20: タイムテーブルの開始時刻を午前6時に変更。一日の活動サイクル（6:00〜翌5:45）に合わせた表示順に調整。
- 2026-04-30 19:30: タイムテーブルに1時間ごとの罫線を追加（00分の行に強調表示）。
- 2026-04-30 19:45: アプリ全体をミューテッド（彩度を抑えた）カラーパレットに刷新。原色を避け、インディゴ、ラベンダー、ミントなどの目に優しいトーンに統一。グラフ配色も同期。
- 2026-04-30 20:25: アクティビティ概要（ダッシュボード）等のカードにホバーした際に上に浮き上がるエフェクト（transform: translateY）を削除。
- 2026-04-30 20:35: CSV出力ボタンを各画面のヘッダーから削除し、サイドバー（ナビゲーション）に移動。期間選択の状態を維持したまま、どの画面からでも即座にエクスポート可能に改善。
- 2026-04-30 20:45: CSV出力ボタンをクリックした際に設定モーダルを表示するように変更。出力期間の個別指定および「すべてのデータを出力」を選択できる機能を追加。
- 2026-04-30 20:50: 変数の初期化順序の不備による ReferenceError（画面が真っ白になる問題）を修正。
- 2026-04-30 20:55: CSV出力モーダルのUX改善。期間指定カード内で日付を選択しても即座にダウンロードされないよう修正し、出力ボタンのレイアウト崩れ（改行）を解消。
- 2026-04-30 21:00: ボタン内のテキストが折り返されないよう `white-space: nowrap` を適用。また、画面幅が狭い場合に日付選択欄が適切に折り返されるようレスポンシブ対応を強化。
- 2026-04-30 21:05: ダッシュボードのカードタイトルやトグルのテキストが折り返されないよう修正。画面幅が不足した際に、カードヘッダー内の要素が適切に折り返されるように調整。

### 実行ファイルのビルド
- 2026-04-30 21:10: デザイン刷新（ミューテッドカラー）およびCSV出力モーダル実装後の再ビルドを実施。
- `npm run build` を実行し、`dist-pulse` フォルダ内の `PulseWork 1.0.0.exe` を更新。

### メニューバーのカスタマイズ
- 2026-04-30 21:50: 不要なメニュー項目（File, Edit, Window, Help）を削除し、開発や表示調整に便利な「View」メニューのみを残すように `electron-core/main.js` を修正。

## 2026-05-01
### エイリアス機能の廃止
- 2026-05-01 09:35: ユーザーの要望により、エイリアス機能をアプリケーション全体から完全に削除。
  - **フロントエンド**: 設定画面の管理UI、ステート、API呼び出し、およびログ一覧での表示ロジックを削除。
  - **バックエンド**: エイリアス関連のAPIエンドポイント、DB初期化コード、集計・ヒートマップ・エクスポート時のエイリアス参照ロジックを削除。
  - **収集ロジック**: ウィンドウタイトルからエイリアスを抽出して記録する処理を削除し、純粋なウィンドウタイトルのみを収集するように変更。
  - **理由**: 機能の複雑化を避け、シンプルな利用形態に戻すため。

### タイムテーブルの内訳表示機能追加
- 2026-05-01 09:45: タイムテーブルのセル（15分単位）をクリックすると、その時間枠に含まれるすべてのログを詳細表示する機能を実装。
  - **フロントエンド**:
    - セルにクリックハンドラを追加し、詳細表示用のモーダルUIを実装。
    - `lucide-react` から `X` アイコンを追加し、モーダルの閉じる操作に対応。
    - 取得した内訳ログをテーブル形式で表示し、正確な時刻、アプリ名、ウィンドウタイトルを確認可能に。
  - **バックエンド**:
    - 新しいAPIエンドポイント `/api/logs/breakdown` を追加。
    - 指定された日付と時間（15分枠）に合致するログを抽出するクエリを実装。
  - **UI/UX**:
    - モーダル内にスクロール可能なログ一覧を表示し、長時間の作業でも全ログを確認可能に。
    - セルホバー時に「クリックで内訳を表示」というヒントを追加し、操作性を向上。
+  - **内訳モーダルの改善**:
+    - アプリごとの「使用割合（％）」と「合計時間（秒）」を算出する要約セクションを追加。
+    - 使用時間の多い順（降順）に自動でソートされるようにロジックを実装。
+    - プログレスバー風の視覚表示を追加し、15分間の中での作業バランスを直感的に把握可能に。
  - **内訳モーダルの改善**:
    - アプリごとの「使用割合（％）」と「合計時間（秒）」を算出する要約セクションを追加。
    - 使用時間の多い順（降順）に自動でソートされるようにロジックを実装。
    - プログレスバー風の視覚表示を追加し、15分間の中での作業バランスを直感的に把握可能に。

### 表示上のウィンドウ名置換機能（フロントエンド完結型）
- 2026-05-01 10:10: ウィンドウ名を管理しやすい別名に置き換えて表示する機能を追加。
  - **フロントエンド**: 設定画面に「表示名置換ルール」の管理UIを追加（キーワード、置換後の名前、部分一致/前方一致/完全一致の選択）。履歴、タイムテーブル（ホバー表示）、内訳モーダル、および統計データの表示時にのみ名前を置換するように `App.jsx` を修正。
  - **フロントエンド追加機能**: キーワード入力時に、過去のウィンドウタイトルをサジェスト（プルダウン選択）できる機能（`datalist`）を追加。また、選択時に背景色が白くなる（ブラウザのオートコンプリート仕様）問題や、テキストカーソル（キャレット）が黒くなる問題を防ぐためCSSを調整し、プルダウン自体の背景色もテーマに合わせるよう修正。
  - **機能拡張**: 表示名の置換時に、専用の「表示色」も指定できる機能を追加。`window_rules` テーブルに `color` カラムを追加し、設定画面からカラーピッカー（レスポンシブ対応）で選択できるようにしました。指定された色はタイムテーブル、履歴、統計リスト等で自動的に反映されます。
  - **バックエンド**: `window_rules` テーブルを作成し、ルールの取得・追加・削除用API (`/api/window-rules`) を追加。
  - **利点**: データ収集時（DB）には元のウィンドウ名をそのまま保存するため、過去のデータを書き換える必要がなくなり、動作が軽量化。また、いつでもルールを追加・削除して即座に表示へ反映できる安全な設計に改善。
NU0[0~0W0_00
 
 
### 実行ファイルのビルド
- 2026-05-01 11:11: 最新の修正（ルール編集機能など）を反映して再ビルドを実施。
- `npm run build` を実行し、`dist-pulse` フォルダ内の `PulseWork 1.0.0.exe` を更新。

## 2026-05-01 11:19 - コード分析（重複・未使用コード調査）
- プロジェクト全体を調査し、重複コード4箇所、未使用ファイル3件、未使用変数/定数/CSS 4箇所、DB残骸1件を特定
- 詳細は code_analysis.md アーティファクトに記載
- 主な発見: collector.jsのshouldLog重複、ルートmain.jsの未使用、fetchWindowRulesとfetchDataのAPI二重呼び出し等

## 2026-05-01 11:24 - コードクリーンアップ実施
### 削除したファイル
- `main.js`（ルート）: 旧エントリーポイント、未使用
- `check_db.js`: デバッグスクリプト
- `electron-core/diag.js`: 診断スクリプト
- `backend/logs.db`: DB残骸

### コード修正
- `collector.js`: 重複`shouldLog = true`削除、未使用`IDLE_THRESHOLD_SECONDS`/`logAlias`削除
- `App.jsx`: 未使用`Cpu`インポート削除、`fetchWindowRules`関数と二重API呼出し統合、ウィンドウタイトル表示JSXを`renderWindowTitle`ヘルパーに共通化
- `App.css`: 未使用`.export-btn`クラス削除
- `.gitignore`: `*.db`と`scratch/`を追加

### ビルド確認
- `npx vite build` 成功

## 2026-05-01 11:43 - GitHubプッシュ
- 最新のコード変更とクリーンアップ結果をリモートリポジトリへ同期

## 2026-05-01 14:30
### 無操作時の表示内容切り替え機能の追加
- アイドル状態のときの記録方法を「アイドル状態」または「アクティブなウィンドウ名」から選択できる機能を追加
  - **バックエンド**: settings に idle_display_mode（デフォルト idle）を追加。collector.js での設定切り替え判定ロジックを実装。
  - **フロントエンド**: 設定画面に「無操作時の表示内容」を切り替えるボタンを追加。

## 2026-05-01 14:48
### アイドル状態の記録のサブテキスト（説明文）更新
- 「アイドル状態の記録」の設定項目について、新機能に合わせて「操作がない時間について、「アイドル状態」として記録するか、アクティブなウィンドウ名のまま記録するかを選択します」という説明文に修正。

## 2026-05-01 14:57
### アクティビティ履歴取得件数の拡大
- 同じウィンドウを長時間開いている場合でもすべてのサンプリングログを確認できるよう、履歴の取得件数上限（LIMIT）を100件から1,000件に拡大。

## 2026-05-01 15:05
### アイドル時間超過時でもアクティブウィンドウのサンプリング継続機能の追加
- アイドル状態の記録がオフになっている場合でも、無操作のまま同じウィンドウを長時間開いている場合にそのままアクティブウィンドウとしてサンプリングごとに履歴へ記録するよう collector.js のロジックを改善。

## 2026-05-01 15:10
### 自アプリ（WorkPulse）の記録除外ロジックの廃止
- 処理を簡略化するため、collector.js から WorkPulse 自身の記録を除外していた isSelf 判定ロジックを完全に削除。

## 2026-05-01 15:35
### タイムテーブル内訳モーダル内の作業割合要約の切り替え機能追加
- タイムテーブル内の15分間セルクリック時に開く「ログの内訳」モーダルに、アプリ名とウィンドウタイトルで集計・表示を切り替えられるトグルを追加。
- ウィンドウタイトルに置換ルールが適用されている場合は、置換後の名前と元タイトルをカッコ付きで適切に表示するよう改善。

## 2026-05-01 15:38
### トグルボタン配置順の変更
- ダッシュボードの「アプリ/ウィンドウ使用分布」およびタイムテーブル内訳の切り替えトグルについて、ユーザーの要望に合わせて「ウィンドウ」が左、「アプリ」が右になるように配置順を入れ替え。

## 2026-05-01 15:40
### 集計単位（ウィンドウ）のデフォルト化
- ダッシュボードのグラフ集計、およびタイムテーブル内訳の作業割合の要約のデフォルト集計単位を、これまでの「アプリ名」から「ウィンドウ」に変更。

## 2026-05-01 15:45
### 内訳モーダル要約部分のテキスト幅制限
- タイムテーブル詳細の「作業割合の要約」内の項目名が長くなりすぎないように、`maxWidth: '350px'` と `ellipsis` を追加して切り詰めるように改善。

## 2026-05-01 15:53
### UI上の専門用語の改善・分かりやすい表現への置き換え
- **ダッシュボード/概要**:
  - 「アクティビティ概要」を「作業状況のまとめ」に変更。
  - 「サマリー」を「現在の状況」に変更。
  - 「総サンプル数」を「合計の記録回数」に変更。
  - 「検知アプリ数」を「使われたアプリの数」に変更。
  - 「サンプリング間隔」の表記を「記録する間隔」に変更。
- **タイムテーブル**:
  - 「タイムテーブル分析」を「時間ごとの活動の記録」に変更。
- **履歴**:
  - 「アクティビティ履歴」を「これまでの作業履歴」に変更。
- **設定画面**:
  - 「サンプリング間隔」を「記録する頻度（秒）」、「データの収集頻度を設定します」を「どのくらいの頻度で作業状況を記録するかを設定します」に変更。
  - 「デフォルトカラー」を「標準の色」、「エイリアス未設定時の表示色」を「表示名変更のルールがないときの色」に変更。
  - 「アイドル状態の記録」を「操作していない時間の記録」に変更。
  - 「アイドル判定しきい値」を「操作していないとみなす時間」に変更。
  - 「無操作時の表示内容」を「操作していないときの表示内容」に変更。
  - 「表示名置換ルール」を「表示名の変更ルール」に変更。
  - 置換ルールの部分一致、前方一致、完全一致の各用語を「キーワードを含む」「キーワードから始まる」「キーワードと同じ」などの平易な日本語表現に変更。
- **ヘルプ**:
  - 用語（エイリアスやタイムテーブル等）の解説をより分かりやすい優しい言葉（表示名の変更ルール、活動の記録、便利なボタンなど）に一斉変更。

## 2026-05-01 15:58
### ヘルプ・ガイドのアップデート
- アプリの現状の機能（タイムテーブルの詳細内訳、表示名の変更ルール、CSV出力の期間指定と全出力機能など）を反映させた内容にヘルプ画面を刷新しました。

## 2026-05-01 19:00
### タイムテーブルの日付フォーマットの変更
- タイムテーブル（時間ごとの活動の記録）の列ヘッダーにある日付の表示形式を、「年」と「月日(曜日)」に分割した2行形式に変更し、視認性を向上させました。



## 2026-05-01 19:40
### 記録の間隔と標準の色の固定化
- **フロントエンド/バックエンド**: 記録する間隔（サンプリング間隔）を10秒、表示名変更ルールがない時の標準の色を `#6366f1` で固定にしました。
  - **設定画面**: 「記録する間隔」と「標準の色」の設定・変更UIを非活性化・削除し、固定値であることを明示。
  - **DB・サーバー初期化**: 起動時に設定データを強制的に10秒と `#6366f1` で上書き保存するように修正。


## 2026-05-01 19:42
### 記録の間隔と標準の色の項目を設定から削除、ヘルプへの追記
- **フロントエンド**: 設定画面から「記録する間隔」と「標準の色」のUI項目を完全に削除。
- **ヘルプ画面**: 「WorkPulseの使い方」内「1. 自動記録」のセクションに、記録間隔（10秒）および標準の色（#6366f1）が固定である旨の注記を追加。
- **ヘルプ画面の修正**: ヘルプ画面から「標準の色」に関する注記を削除し、「記録する間隔」のみを記載。
- **設定画面の修正**: 操作していない時間の記録のトグルボタンがテキストに押し出されてズレる問題を修正（Flexboxの幅制限とマージン調整）。

## 2026-05-02
### タスクトレイ格納機能および自動記録開始の追加
- **メインプロセス**: タスクトレイ（システムトレイ）アイコン機能の追加。ウィンドウを閉じようとした際に終了せずタスクトレイに格納（非表示）するようにし、トレイメニューから「表示」「終了」を実行できるように修正。
- **自動記録開始**: アプリケーション起動時に、確認ダイアログを出さずに自動でバックグラウンドログ収集（collector）を開始するように変更。

### コレクターのバグ修正
- **コレクター（ログ収集ロジック）**: `windowTitle` または `appName` が `null` の場合に、文字列抽出（`.substring`）による TypeError が発生する問題を修正（`null`/`undefined`時のデフォルトフォールバックを実装）。

### タスクトレイ・終了機能のアップデート
- **メインプロセス**: タスクトレイ（システムトレイ）アイコンをクリック（シングルクリック）した際にも、ウィンドウが表示されるようにイベントハンドラを追加。
- **フロントエンド**: サイドバー下部に「アプリを完全に閉じる」ボタンを追加し、クリック確認時にアプリ全体と収集バックグラウンドプロセスを完全に終了する機能（`app:quit-completely` IPCハンドラ）を実装。

### ウィンドウ・アプリ一覧の表示件数＆文字数制限
- **フロントエンド**: 統計表示におけるウィンドウ名、アプリ名の文字数を最大20文字に制限し、はみ出しを防ぐよう省略処理（`...`）を実装。
- **グラフとリストの表示制限**: グラフおよび進捗バー付きリストに表示される最大件数を6件に制限。6件目以降は「その他」として合算し、グラフ描画や凡例のレイアウト崩れを防ぐ改善を実施。

### タイムテーブルのデフォルト表示日数の変更
- **フロントエンド**: タイムテーブルおよびダッシュボード全体のデフォルトの表示日数（初期値）を、当日の週の日曜日から土曜日までに変更。

## 2026-05-02 10:45
### ログ記録の負荷に関する質問への回答
- 無操作時（アイドル状態）のログ記録有無によるCPU・ディスク負荷の比較解説を提供。

## 2026-05-02 10:52
### 無操作時（アイドル）の記録除外と設定UIの廃止
- **バックエンド/コレクター**: 無操作時にログを完全に記録しない（スキップする）仕様に変更し、不要な記録継続ロジックを廃止。
- **フロントエンド**: 設定画面から「操作していない時間の記録」トグル、「操作していないときの表示内容」ボタンを削除。無操作判定しきい値の設定のみを維持するようにUIを統合。
- **ヘルプ画面**: 無操作時の判定について、負荷軽減のために記録が停止される旨を追記。

## 2026-05-02 10:56
### GitHubへのプッシュ
- これまでの変更内容をコミットし、リモートのGitHubリポジトリ（`main` ブランチ）へプッシュ。


## 2026-05-02 11:10
### コード全体のクリーンアップ（無駄なコード・処理の削除）
- **electron-core/main.js**:
  - 重複IPCハンドラー `recording:confirm-start` を削除（`recording:start` と完全同一のため）
  - タスクトレイの `double-click` イベントハンドラーを削除（`click` と同一処理のため）
- **backend/server.js**:
  - `sampling_interval` と `default_activity_color` の初期化を `INSERT OR REPLACE` → `INSERT OR IGNORE` に変更（サーバー再起動時にユーザー設定がリセットされるバグを修正）
  - 不要な `ALTER TABLE` マイグレーション（colorカラム追加）を削除（CREATE TABLE時に含まれている）
  - Alias削除時の残骸コメントを削除
- **backend/collector.js**:
  - 未使用の設定取得（`recordIdle`, `idleDisplayMode`）を削除
- **frontend/src/App.jsx**:
  - 未使用import `Timer`, `Menu` を削除
  - fetchData内の設定ハードコード上書き（sampling_interval, default_activity_color）を削除（サーバーの値を正しく使うように修正）
  - 起動時自動記録useEffect全体を削除（main.jsで既に自動起動済みのため二重起動だった）
  - 未使用state `isSaving` と ref `hasPrompted` を削除
  - コメントアウトされた `<Cpu>` コンポーネントの死コードを削除
  - Alias関連の空コメントを削除
- **package.json（ルート）**: 未使用パッケージ `electron-is-dev`, `electron-packager` をアンインストール
- **frontend/package.json**: 未使用パッケージ `typescript` をアンインストール
- **ビルド確認**: `npx vite build` 成功

## 2026-05-02 12:10
### 疲労度算出・表示とミニウィンドウ機能の追加
- **バックエンド**:
  - `GET /api/fatigue` エンドポイントを実装。
  - 本日の最初のログからの経過時間とサンプリング期待値に基づいてアイドル率・疲労状態（Chill/Good/Busy/Danger）を算出。
- **メインプロセス（main.js）**:
  - ミニウィンドウ用の `mini-window:open` / `mini-window:close` IPCハンドラーを追加。
- **フロントエンド**:
  - `App.jsx` に疲労度データ取得（`fetchData`）と、ダッシュボードへの「疲労状態」カードの表示機能を追加。
  - URLのクエリパラメータ `?mini=true` を判別するミニ表示ウィジェット（D&D移動可能・最前面表示）を実装。
  - メイン画面とミニウィンドウを相互に切り替えられるボタンを追加。

## 2026-05-02 12:20
### 親画面非表示時の自動ミニ画面表示と表示位置詳細設定の追加
- **バックエンド**: `settings` テーブルのデフォルト値として `show_mini_on_close` と `mini_window_position` を追加。
- **メインプロセス（main.js）**: 親画面の「X」によるクローズ時に、自動表示設定（`show_mini_on_close`）が `true` の場合のみミニ画面を表示。表示位置（左上、右上、左下、右下）に応じた座標計算ロジックを追加。
- **フロントエンド**: 設定タブとミニ画面に「親画面を閉じたら表示」と「ミニ画面の初期表示位置」の制御コンポーネントを追加。オフにした場合はミニ画面が即時終了する処理を実装。

## 2026-05-02 12:28
### ミニ画面の設定箇所の限定とデフォルト設定の変更
- **フロントエンド**: 表示場所の選択（mini_window_position）を親ウィンドウ（設定タブ）のみから設定できるように制限（子ウィンドウからは非表示）。
- **バックエンド**: 自動表示設定のデフォルト値を `表示する`、初期位置のデフォルト値を `右下` に変更し、既存の `右上` 設定を自動的に `右下` へ補正。

## 2026-05-02 12:43
### ミニ画面内への作業アドバイスメッセージの表示
- **フロントエンド**: ミニ画面の進捗バーの下に、疲労度に応じたアドバイスメッセージ（「かなり余裕があります。マイペースに進めていきましょう！」など）を表示するブロックを追加。

## 2026-05-02 12:46
### メイン画面最小化時にもミニ画面を表示する処理の追加
- **メインプロセス（main.js）**: メイン画面が最小化（`minimize`）された際、自動表示設定（`show_mini_on_close`）が `true` であればミニ画面を表示するリスナーを追加。また、メイン画面が元に戻されたり表示された際、自動的にミニ画面を閉じる `restore`、`show` リスナーを追加。

## 2026-05-02 12:53
### 使用アプリ数・使用ウィンドウ数の表示調整とカウントダウン廃止
- **フロントエンド**: 
  - ダッシュボードの「使われたアプリの数」を「使用したアプリの数」に表示名を変更。
  - ダッシュボードの「現在の状況」から「次の記録まで 約10秒」を廃止。
  - 新たな統計カード「使用したウィンドウの数」を追加。
  - フロントエンド側でアプリ・ウィンドウ別それぞれの全件データを集計することで、表示制限等に影響されない正確な利用件数の算出を実現。

## 2026-05-02 12:56
### 3項目のカードを縦並びに変更
- **フロントエンド**: 
  - ダッシュボードの「現在の状況」カード内にある3つの統計情報を、見やすく縦に配置するように変更しました。
  - 各項目を横幅いっぱいに広げ、項目名と数値を左右に配置（flex）することで、テキストの折り返しを防止して視認性を高めました。

## 2026-05-02 13:02
### 合計記録回数のフォーマット表示（999超過時）
- **フロントエンド**: 
  - 「合計の記録回数」が999回を超えた場合に、K、M、G、Tのサフィックスを付与してコンパクトに表示するように対応。
  - 小数点第一位で繰り上げ（Math.ceil）を行い、視認性を向上。

## 2026-05-02 13:10
### 関数のコンポーネント外部化と一時変数の削減
- **フロントエンド（App.jsx）**:
  - `applyWindowRules`, `renderWindowTitle`, `formatNumberWithSuffix`, `getWeekRange` のような、コンポーネント内のステートに直接依存しないユーティリティ・純粋関数を `App()` コンポーネントの外部に移動。
  - レンダリング負荷とメモリ消費効率（再生成の抑制）を向上。
  - `fetchData` 内にあった不要な一時変数 `finalSettings` の宣言を削除し、直接 `setSettings` に渡すようにリファクタリング。
  - ビルド確認・動作確認を完了。

## 2026-05-02 13:14
### ヘルプ・ガイドを最新機能に合わせて更新
- **フロントエンド（App.jsx）**:
  - ヘルプ・ガイドの内容を、最新機能（サンプリング間隔の自由変更、ミニウィンドウ自動表示、リアルタイムの疲労状態・アイドル率算出機能）に適合するように刷新しました。

## 2026-05-02 13:31
### 疲労状態Danger時のアラート機能の実装
- 「疲労状態」がDangerになったらデスクトップ通知（OS標準通知）を出す機能の実装を完了しました。
- UIの変更は行わず、バックグラウンドでの判定および通知の実行のみを行うように調整しました。

## 2026-05-02 14:36
### 疲労状態Danger時のポップアップダイアログ通知の追加
- OSの通知がオフの場合でも確実に気づくことができるよう、`Danger`状態への遷移時にElectronのネイティブなメッセージボックス（`dialog.showMessageBox`）が前面にポップアップする機能を実装しました。

## 2026-05-02 17:08
### 稼働開始直後の判定猶予（30分）の追加とリセット機能の実装
- **バックエンド**: 
  - 本日のログ計測開始から30分（1800秒）以内であれば、アイドル率に関わらず疲労状態のDanger判定をGoodへ変更する猶予期間を導入。
  - `POST /api/fatigue/reset` エンドポイントを実装し、本日の全ログデータを消去して作業開始時間（計測基準）をリセット可能に。
- **フロントエンド**: 
  - 疲労状態カード、およびミニ画面それぞれに「リセット」ボタンを配置。

- **バックエンド**: `GET /api/fatigue` で取得する `MIN(timestamp)`（UTC文字列）を、レスポンス前にSQLiteの `datetime(?, 'localtime')` で現地時刻に変換し返却するよう修正。フロントエンドでの時刻解釈エラー（9時間のズレ）を解消。

## 2026-05-02 17:18
### 疲労度リセット機能の完全一致・タイムゾーンズレ修正
- **バックエンド**:
  - 本日のログデータ消去（リセット）時に、SQLiteの関数依存を廃止し、Javascriptの `YYYY-MM-DD` 形式（ローカル・UTC両対応）による `LIKE` 検索で確実に本日分の全件を消去するように修正。
  - レスポンスする `startTime` に `Z` サフィックスを付与することで、Javascript標準の `Date` オブジェクトが自動でローカルタイムに変換・表示するよう安定化。

## 2026-05-02 17:40
### 作業開始直後のステータス名を「Flesh」に変更
- **バックエンド**: 稼働開始から30分以内（1800秒未満）の場合の疲労状態判定を `Good` から `Flesh` に変更しました。
- **フロントエンド**:
  - メイン画面の疲労状態カードおよびミニ画面における `statusName === 'Flesh'` の場合のアドバイスメッセージ（「まだ作業を始めたばかりです。この調子で進めましょう！」）を追加しました。

## 2026-05-02 17:45
### バックエンド重複プロセスの解消
- **トラブルシューティング**: 以前のセッション（タスクトレイ内の旧プロセスなど）で立ち上がっていた `PulseWork.exe` がポート `3001` を握り続けていたため、コード変更が反映されないバグが発生。古いバックグラウンドプロセスを強制終了させて解消しました。

## 2026-05-02 17:55
### リセットボタンの配置変更
- **フロントエンド**: メイン画面の疲労状態カード、およびミニ画面内に配置されていた「リセット」ボタンを削除。設定画面（設定タブ）内の「記録の削除とリセット」カードへと一本化しました。これにより、誤操作の防止と設定の一貫性を実現しました。

## 2026-05-02 18:05
### データがないときのステータスを「Flesh」に変更
- **バックエンド**: 本日の作業ログが1件も存在しないとき、ステータスを `Chill` から `Flesh` に変更し、アイドル率を `0` に設定しました。これにより、一日の作業開始前やリセット直後の表示が違和感のない状態になります。

## 2026-05-02 18:10
### 疲労状態のアドバイスメッセージに改行を追加
- **フロントエンド**: メイン画面・ミニ画面のアドバイスメッセージに改行（`\n`）を挿入し、スタイルに `whiteSpace: 'pre-line'` を追加してメッセージの視認性を高めました。

## 2026-05-02 18:14
### コード全体のクリーンアップと無駄な処理・ファイルの削除
- **ルート**: 開発時にのみ使用され現在は無駄となっていた `test-db.js` を削除しました。
- **バックエンド**: `settings` テーブルの初期化処理から、過去に廃止された不要なキー（`record_idle`, `idle_display_mode`）の挿入処理を削除し、DB初期化を軽量化しました。

## 2026-05-02 18:18
### GitHubへのプッシュ
- これまでのコードクリーンアップ内容をリモートのGitHubリポジトリ（`main` ブランチ）へ同期。

## 2026-05-02 18:30
### アプリケーション仕様書の作成
- **仕様書作成**: `specification.md` に、アプリの全体概要、システム構成（Mermaid図）、データベース設計、収集・判定アルゴリズム、UIの機能解説、開発・保守手順、トラブルシューティングなどを詳細に記述し、後任者が引き継げるように仕様書を新規作成しました。

## 2026-05-02 20:53
### 疲労度アラートの改善
- **フロントエンド**: 疲労状態（Danger）のアラート通知文に、Goodになるまでの休憩時間（10の位で繰り上げ）を表示するロジックを実装。`activeLogs` と `expectedLogs` から15%以上のアイドル率（Good条件）を満たすのに必要な休憩時間を算出し、10分単位に繰り上げる（切り上げる）ことで、具体的な休憩時間の提案をアラート文章に含めました。

## 2026-05-02 23:07
### 集計開始猶予時間（Flesh状態）の変更
- **バックエンド**: その日の集計開始猶予時間（Flesh状態の時間）の判定基準を30分（1800秒）から60分（3600秒）へ変更しました。

## 2026-05-03 08:23
### ミニ画面の表示位置の拡張
- **フロントエンド**: 設定画面の「ミニ画面の初期表示位置」セレクトボックスに、「上」「左」「右」「下」の4方向を追加。
- **メインプロセス**: 位置決め座標計算ロジックを修正し、「上」「左」「右」「下」に対応する中央配置などの座標（`workAreaSize`準拠）を実装。

## 2026-05-03 08:29
### 作業開始時間の当日限定化
- **バックエンド**: 疲労度測定およびリセットの基準とするログデータの取得範囲を、SQLiteの `date(timestamp, 'localtime') = date('now', 'localtime')` を用いて「当日の現地時刻（JST）」に厳密に制限。これにより前日の作業開始時間が当日分として誤認される問題を完全に解消。

## 2026-05-03 08:42
### 疲労状態Flesh時の表示調整
- **フロントエンド**: 疲労状態が `Flesh` の場合、アイドル率の数値を「集計中. . .」として表示し、疲労度ゲージの幅を 0% にするよう `App.jsx` を修正（メイン画面およびミニ画面の両方）。

## 2026-05-03 09:34
### 疲労アラートの5分おき通知の実装
- **フロントエンド**: `App.jsx` の疲労度取得ロジックに `lastAlertTimeRef` を導入し、疲労状態が `Danger` である間は5分おきに通知を出すよう改修。

## 2026-05-03 09:53
### アイドル率から稼働率への表記変更
- **フロントエンド**: `App.jsx` 内の「アイドル率」の表記を「稼働率」に変更。表示される数値も `100 - fatigueData.idleRate` となるよう計算方法を修正（メイン画面・ミニ画面、ドキュメント・ヒント表示も含む）。

## 2026-05-03 09:57
### 「メイン画面を閉じたら表示」をメイン画面にも追加
- **フロントエンド**: ミニ画面のみに表示されていた「メイン画面を閉じたら表示」チェックボックスを、メイン画面の疲労ゲージカードのヘッダーにも追加・同期。

## 2026-05-03 09:59
### 疲労ゲージカードのヘッダーのレスポンシブ化
- **フロントエンド**: メイン画面の疲労ゲージカードのタイトル部分に `flexWrap: 'wrap'` と `gap` を指定。チェックボックスとボタンが狭い画面でも美しく折り返されるように修正。

## 2026-05-03 10:33
### ミニ画面の次回表示位置保持
- **メインプロセス**: ミニ画面が移動されたとき（`move`イベント発生時）に、その座標を `settings` テーブル（`mini_window_x`, `mini_window_y`）に保存するよう変更。次回起動時は前回終了時の座標で表示するように修正。
- **フロントエンド**: 設定画面内の「ミニ画面の初期表示位置」の選択項目を廃止・削除。

## 2026-05-03 10:37
### 設定画面からミニ画面設定セクションの削除
- **フロントエンド**: 疲労ゲージカード等に操作ボタン・チェックボックスが集約されたため、設定画面の「ミニウィンドウ（ウィジェット）の設定」カードを完全に削除。

## 2026-05-03 11:00
### コレクターのサンプリング間隔取得バグの解消
- **バックエンド（collector.js）**:
  - 設定画面から変更した「サンプリング間隔（`sampling_interval`）」が、バックグラウンドでのアクティブウィンドウの記録時に無視され10秒に固定されていた不具合（非効率・無駄）を解消。
  - 設定値を取得して記録インターバルに正しく反映するようにリファクタリングを実施。

## 2026-05-03 11:04
### ヘルプ・ガイドに最新アラート機能の解説を追加
- **フロントエンド**: ヘルプ・ガイド内に、疲労状態が Danger のときに出る警告アラートおよび「メイン画面を閉じたら表示」機能についての解説文を追加。

## 2026-05-03 11:09
### ヘルプ・ガイド内の警告アラート間隔の表記修正
- **フロントエンド**: コードに合わせ、ヘルプ内の「5分おきに休憩を促す」という表記を実際のプログラムの挙動に準拠した「10分おきに休憩を促す」へと修正しました。

## 2026-05-03 11:43
### ヘルプ・ガイド内のアラート間隔とデータ保存場所の変数化
- **フロントエンド**: ヘルプ・ガイド画面のアラート通知間隔の記述（10分）とデータベースおよびアプリケーション設定保存先フォルダパスの記述（logs.db, workloggerapp）を、コード内の状態と一致するように変数・定数（`FATIGUE_ALERT_INTERVAL_MINUTES`, `DB_NAME`, `APP_DIR_NAME`）を埋め込む形式に修正し、動的に同期されるように改善しました。

## 2026-05-03 11:54
### タイムテーブル見出し行（日付行）の背景の完全不透明化
- **フロントエンド（App.css）**: `.day-label` に `background: var(--sticky-bg);` を追加し、さらに土日の `.is-saturday`, `.is-sunday` には透過色を solid な `--sticky-bg` と組み合わせる `linear-gradient` 方式を適用することで、見出し行が透けて表示されないように完全に不透明化しました。

## 2026-05-03 12:22
### ミニ画面内への疲労度連動ペット表示機能の検討
- ミニ画面（ウィジェット）内に疲労ゲージの数値（ステータス）に応じて動くペットを表示させ、表示切り替えを設定できる機能の実装プランを提示。

## 2026-05-03 12:26
### ペットのデザイン形式の確認
- AA（顔文字）と、SVG/絵文字を使ったふわふわ動くアニメーション表現の2パターンを提案し、方針を確認。

## 2026-05-03 12:28
### CSSアニメーションの負荷評価
- CSSアニメーション（`transform`や`opacity`）はGPUアクセラレーションが効くため、メモリ・CPU負荷が極めて低いことを説明。

## 2026-05-03 12:30
### ミニ画面へのペット表示機能の実装
- **バックエンド**: `settings` テーブルの初期値に `show_pet_in_mini`（デフォルト: `true`）を追加。
- **フロントエンド**: 
  - `App.css` にペット用のCSSアニメーション定義（Bounce / Float / Fade / Shake / Pulseなど）を追加。
  - `App.jsx` のダッシュボードの疲労ゲージカード内とミニ画面のフッターに、「ペットを表示」チェックボックスを追加し、オンオフ設定の切り替えに対応。
  - `App.jsx` のミニ画面にペットを表示するコンポーネントを追加。

## 2026-05-03 12:35
### ペットのSVGアイコン化（スライム風）の対応
- ミニ画面의ペット表示を絵文字からSVGコンポーネント（表情豊かなスライム風のキャラクター）へ差し替え、リッチなアニメーション表現を実現。

## 2026-05-03 12:46
### ペット領域のレイアウト調整、セリフ削除、スクロール防止
- ペットを表示・非表示切り替えても全体のレイアウト（高さ）が変わらないように、ペット表示用のコンテナ領域（高さ36px）を固定で確保。
- ペットのセリフ（テキスト）を削除。
- ミニ画面内が上下スクロールしないよう、各要素のサイズとマージンをコンパクト化。

## 2026-05-03 12:52
### ペットのステータス横並び配置化とメイン画面トグルの削除
- **フロントエンド**:
  - ミニ画面において、ペット（SVG）をステータス・稼働率の右隣に横並びで表示するように変更。これにより縦幅を大幅に削減しスクロールの心配を完全に解消。
  - メイン画面のダッシュボードにある「ミニ画面にペットを表示」チェックボックスを削除。

## 2026-05-03 12:55
### ペットを上部ヘッダー（疲労ゲージタイトルの右隣）へ移動
- **フロントエンド**: ミニ画面の上部ヘッダー内、「疲労ゲージ」タイトルの右隣にペット（SVG）を表示するように配置変更。

## 2026-05-03 13:13
### ペットを疲労状態（ステータス）の右隣に移動
- **フロントエンド**: ミニ画面のヘッダーにあったペットを非表示にし、中段の「疲労状態（Danger、Goodなど）」＋稼働率表示のすぐ右隣に表示するように配置変更。

## 2026-05-03 13:18
### ペット表示切り替え時のレイアウト崩れ防止対策
- **フロントエンド**:
  - ペットの表示コンテナに固定幅（`width: 40px`, `height: 40px`）を指定。
  - 表示切り替え時に `visibility` 属性（`visible` / `hidden`）を適用することで、非表示時もスペースが維持され、レイアウトが一切崩れないように修正。

## 2026-05-03 13:53
### ミニ画面にペットの全パターン確認用デバッグボタンを追加
- **フロントエンド**:
  - ミニ画面の下部（稼働時間の右隣）に、ペットの全パターン（Flesh, Good, Chill, Busy, Danger）を強制的に切り替えて確認できるデバッグボタングループ（`F`, `G`, `C`, `B`, `D`）を追加。

## 2026-05-03 14:04
### Danger状態のペット（スライム）の強化
- **フロントエンド**:
  - Danger状態のペット（スライム）の揺れる速度をさらに高速化（`pet-danger-shake`を0.4秒に設定）。
  - 右斜め上に汗の滴る描写（SVGパスによる水色水滴）を追加。

## 2026-05-03 14:11
### ペット確認用デバッグボタンの削除
- **フロントエンド**:
  - 全パターン確認用として追加していたデバッグステート、およびデバッグ確認ボタン群（`F`, `G`, `C`, `B`, `D`）を削除。

## 2026-05-03 14:14
### ミニ画面の角丸透過対応
- **フロントエンド**:
  - ミニ画面モードでの表示時に、`useEffect` を使って最上位の `body` および `html` 要素の背景を完全に透過（`background: transparent`）、および角丸（`borderRadius: 16px`）を適用。
  - 四角い背景の重なりを解消し、ミニ画面の角丸がElectron上で綺麗に見えるように修正。

## 2026-05-03 14:44
### タイムテーブル各セルの詳細に合計時間を追加
- **フロントエンド**:
  - タイムテーブルのセルのホバー時ツールチップ（`title`）に、サンプリング数に基づく合計時間を表示する機能（例: 「合計時間: 2分30秒」）を追加。
  - 「ログの内訳」モーダルのヘッダーに、該当15分枠における全件ログからの合計時間（例: 「合計記録時間: 2分30秒」）をリアルタイムで表示するように修正。

## 2026-05-03 15:40
### 過集中防止のための90分スライディングウィンドウ方式と5段階状態判定の導入
- **バックエンド**:
  - 疲労度計算ロジック（`GET /api/fatigue`）において、直近90分間の経過時間とログ数を基にした「90分スライディングウィンドウ方式」に変更。
  - 過去の長時間の休息が直近の過集中（作業継続）を隠蔽してしまう課題を解決。
  - 猶予時間（30分）とアイドル率に基づき、5段階の状態（Flesh, Chill, Good, Busy, Danger）へのマッピングに拡張。
- **仕様書（specification.md）**:
  - 「5. 疲労度算出アルゴリズム」の節を新しい仕様（90分スライディングウィンドウ方式、5段階判定）にアップデート。
- **フロントエンド（App.jsx）**:
  - ヘルプ・ガイド画面に「90分スライディングウィンドウ方式」についての解説を追記。
  - 作業開始から30分経過後は、`Flesh` 状態であっても「集計中...」ではなく正しい稼働率が表示されるように改善。
- **バックエンド（server.js）**:
  - 猶予時間（30分未満）の期間を `Calculating`（集計中）状態として5つのステータスとは独立した状態として分離・定義。
- **フロントエンド（App.jsx）**:
  - `Calculating` 状態時のアドバイスメッセージ、ゲージの幅（0%）、集計中ラベルの出し分け処理を追加。
  - ダッシュボードとミニウィジェットで重複していたアドバイスメッセージ表示処理を、共通の純粋関数 `getFatigueAdvice` に抽出して共通化。

## 2026-05-03 18:00
### マインドフルネスを軸とした6段階の疲労状態名への全面刷新
- **バックエンド（server.js）**:
  - 猶予期間（30分）経過後の5つの疲労状態名をCase 3（`Restored`, `Calm`, `Focused`, `Strained`, `Critical`）に変更。
- **フロントエンド（App.jsx）**:
  - 各種ステータスの表示名や、アドバイス文、色付け判定、ウィジェットのアラートトリガーなどをCase 3へ移行。
  - ヘルプ画面内の解説文、各種機能概要も新しい疲労状態名（マインドフルネス軸）に更新。
- **仕様書（specification.md）**:
  - 「5. 疲労度算出アルゴリズム」の節をマインドフルネスを軸とした新状態名に刷新。

## 2026-05-03 18:18
### Restored状態のアドバイス文のアップデート
- **フロントエンド（App.jsx）**:
  - `Restored` 状態のメッセージ文言をより新鮮な気持ちを表すものに改善。

## 2026-05-03 18:21
### ミニ画面内ペットの疲労状態判定のバグ修正
- **フロントエンド（App.jsx）**:
  - ミニ画面内ペットのステータス判定が古いままだったため、新状態名（`Restored` 等）へ刷新し、正常な表情・カラーが表示されるように修正。

## 2026-05-03 18:25
### ミニ画面に新しい6つのステータス確認用デバッグボタンを追加
- **フロントエンド（App.jsx）**:
  - 新しく刷新された6つのステータス名（Calculating, Restored, Calm, Focused, Strained, Critical）をテスト表示・確認できるデバッグボタングループをミニ画面下部（稼働時間の右隣）に追加。

## 2026-05-03 18:32
### Strained状態のペットにも汗の描写を追加
- **フロントエンド（App.jsx）**:
  - メイン画面とミニ画面の双方のペット（スライム）描画ロジックにて、`Strained` 状態の際にも `Critical` 状態と同じ水色の汗が吹き出るSVGパス描写を追加。

## 2026-05-03 19:02
### Calculating（集計中）専用のペット描画・アニメーションを追加
- **フロントエンド（App.jsx）**:
  - `Calculating` 状態のペットが `Critical` の条件分岐に入ってしまっていたのを修正。
  - `Calculating` 専用の穏やかなパルスアニメーション（`.pet-pulse`）、思索中を表す表情（きりっとした横棒の目＋口）、および落ち着いたカラー（`#94a3b8`）をメイン画面・ミニ画面両方に追加。

## 2026-05-03 19:08
### FocusedとBalancedのアニメーションの再割り当て（状態への最適化）
- **フロントエンド（App.jsx）**:
  - `Focused`（集中・順調）の時は深く呼吸して集中を表すように `.pet-pulse` に変更。
  - `Calm`（マイペース・ゆったり）の時はリラックスして浮遊感を表すように `.pet-float` に変更。
  - `Restored`（元気）はリズミカルに弾む `.pet-bounce` を維持し、マインドフルネス軸と動きの意味を完全に一致させました。

## 2026-05-03 19:15
### Focused状態のペットの動き抑制とキラキラ描写追加
- **フロントエンド（App.jsx, App.css）**:
  - `Focused`（集中・順調）状態の動きをごく控えめな伸縮に抑えた新しいアニメーション `.pet-pulse-subtle` を追加し、ペットに適用。
  - スライムの周囲に星・光を表現するレモンイエローのSVGパーツ（キラキラ描写）を追加し、キラキラと輝く瞬きアニメーション（`.pet-sparkle`）を適用。

## 2026-05-03 19:20
### Balancedの口の形状変更
- **フロントエンド（App.jsx）**:
  - メin画面とミニ画面の双方において、`Calm` 状態のペットの口の形状を `Focused` 状態と同じ笑顔・楽しげな形状に変更。

## 2026-05-03 19:24
### Strainedの口の形状変更
- **フロントエンド（App.jsx）**:
  - `Strained` 状態の口の形状を、`Critical` 状態の波型で焦っている口の形状に揃えました。

## 2026-05-03 19:27
### デバッグボタンの削除
- **フロントエンド（App.jsx）**:
  - 各パターンの切り替え確認用デバッグボタン（ボタングループ）および不要になったステート変数を削除しました。

## 2026-05-03 19:48
### 無操作（アイドル）判定設定の検討
- 設定画面の「無操作の判定設定」および疑似無操作判定についての仕様・折衷案の検討を行いました。

## 2026-05-03 20:04
### 設定画面「無操作の判定設定」の目安追記
- **フロントエンド（App.jsx）**: 「無操作の判定設定」にワークスタイル（短期離席・オフィスワーク・クリエイティブ作業）別の推奨秒数の目安を追記しました。

## 2026-05-04 08:32
### ミニ画面の稼働率・集計中表示の配置調整
- **フロントエンド（App.jsx）**: ミニ画面にて「Calculating」や「集計中...」などの文字列が入り切らない問題を解決するため、稼働率・集計中ラベルをステータス（状態表示）の下に移動し、`flexDirection: 'column'`による縦並び配置へ変更しました。

## 2026-05-04 10:08
### 疲労状態名BalancedからCalmへの全面移行
- **フロントエンド（App.jsx）**: ステータス判定やヘルプ画面の記述に存在していた「Balanced」の名称を、バックエンド側の実装に合わせてすべて「Calm」へ完全に統一・移行しました。

## 2026-05-04 10:20
### メイン画面のダッシュボードへのペット表示追加とコンポーネント共通化
- **フロントエンド（App.jsx）**: メイン画面のダッシュボード（疲労ゲージカード）のステータス名の右側にもペットを表示しました。また、ペットの描画ロジックを `PetIcon` コンポーネントとして抽出し、メイン画面とミニ画面で共通利用できるようにコードを整理しました。

## 2026-05-04 11:28
### ぶっ続け作業時の稼働率が100%にならない問題の解消
- **バックエンド（collector.js）**: サンプリング周期（`setTimeout`）にPowerShellスクリプトの実行時間を含めないよう、実行開始時刻を基にした正確な次回のスケジュール調整を導入。
- **バックエンド（server.js）**: 90分スライディングウィンドウ内での期待ログ数の算出に使う間隔（`samplingInterval`）を、実際のサンプリング周期の実測値（約11秒）に補正。
- **効果**: ぶっ続けで作業していても稼働率が100%に正しく到達しやすくなるよう改善。

## 2026-05-04 12:15
### メイン画面を閉じた際に二重にアラートが表示される不具合の修正
- **フロントエンド（App.jsx）**: メイン画面を閉じてミニ画面に切り替えた瞬間（またはミニ画面がマウントされた瞬間）に `prevStatusRef` が初期化され、再度アラートが発火してしまう問題を解決するため、ミニ画面（`isMiniMode`）ではアラートを抑制する条件を追加。
- **効果**: メイン画面を閉じたりミニ画面へ移行する際の煩わしい確認アラートを防止。

## 2026-05-04 12:20
### 疲労アラートの定期通知（10分おき）の廃止
- **フロントエンド（App.jsx）**: 疲労状態が `Critical` のときの10分おきの定期アラート処理を削除し、ステータスが `Critical` に切り替わった最初の1回のみアラートが表示されるように改善。
- **効果**: 作業に集中している際の過剰なアラートの煩わしさを軽減。

## 2026-05-04 12:31
### 無操作判定時間を120秒（2分）に固定し、設定UIを廃止
- **バックエンド（server.js）**: アプリ初期化時に `idle_threshold` の設定を自動的に120秒へ更新・挿入するように変更。
- **バックエンド（collector.js）**: 無操作時間を120秒（2分）固定で処理するようにハードコード。
- **フロントエンド（App.jsx）**: 「無操作の判定設定」のスライダーUIを完全に削除。
- **効果**: 操作を停止して2分後に速やかに離席（アイドル状態）と判定されるようになり、体感に沿った正確な稼働率計算が可能に。

## 2026-05-04 12:38
### ヘルプガイドおよび仕様書（specification.md）の最新化
- **フロントエンド（App.jsx）**: ヘルプ画面内の「疲労アラートの通知頻度」および「無操作判定の固定時間（120秒）」の説明を最新仕様に更新。
- **仕様書（specification.md）**: 120秒の固定化、および疲労アラートの通知仕様（最初の一回のみ）について仕様書の記述を網羅的に更新。
- **効果**: ユーザー向けのガイドや、開発者向けのドキュメントの整合性を確保。

## 2026-05-04 12:40
### 不要な変数およびロジックのリファクタリング
- **フロントエンド（App.jsx）**: 疲労アラート通知頻度の定数 `FATIGUE_ALERT_INTERVAL_MINUTES` を削除。また、もう使用されなくなった `lastAlertTimeRef` と、`handleSaveSetting` 内の `idle_threshold` の上限バリデーション等の無駄なロジックを完全に削除。
- **効果**: コードがスッキリし、メンテナンス性とパフォーマンスが向上。

## 2026-05-04 16:50
### ミニ画面のライトモード/ダークモード対応
- **フロントエンド（App.css）**: `.dark` / `.light` のCSS変数定義にミニウィンドウ専用の変数群（`--mini-bg`, `--mini-border`, `--mini-text`, `--mini-text-heading`, `--mini-text-sub`, `--mini-text-dim`, `--mini-btn-bg`, `--mini-btn-border`, `--mini-btn-text`, `--mini-bar-bg`, `--mini-divider`）を追加。
- **フロントエンド（App.jsx）**: ミニ画面のインラインスタイルにハードコードされていたダーク用の色（`rgba(30, 41, 59, 0.95)`, `#cbd5e1`, `#fff`, `#94a3b8`, `#64748b` 等）をすべてCSS変数に置き換え。
- **効果**: メイン画面と同様に、ミニ画面もライトモード・ダークモード・システム設定に自動追従するように改善。

## 2026-05-04 20:30
### 疲労アラートの休憩時間表記の削除
- **フロントエンド（App.jsx）**: 疲労状態（Critical）のときに表示される疲労アラートの文章から休憩時間の表記（分単位）を削除。
- **効果**: よりシンプルでスッキリとした警告アラートメッセージに変更。

## 2026-05-04 20:53
### 疲労アラートが最前面に表示されるように修正
- **メインプロセス（main.js）**: 疲労アラート（alert:danger）を表示する際に対象のウィンドウ（メイン画面またはミニ画面）をリストア、表示、およびフォーカスし、さらに一時的に `alwaysOnTop(true, 'screen-saver')` に設定することで、アラートダイアログが確実に最前面にポップアップするように修正。
- **効果**: ユーザーが確実にアラートに気付くように改善。

## 2026-05-04 21:08
### スライディングウィンドウ方式を60分に変更
- **バックエンド（server.js）**: 直近の疲労状態・稼働率の算出範囲を90分から60分へ変更。
- **フロントエンド（App.jsx）**: ヘルプ画面内の表記を「60分スライディングウィンドウ方式」に更新。
- **仕様書（specification.md）**: 仕様書内の「90分」の記述をすべて「60分」に更新。
- **効果**: より直近の活動状態（60分）に即した迅速な判定を行えるように改善。

## 2026-05-05 00:39
### メイン画面非表示時（ミニウィンドウのみ）の疲労アラートの正常化
- **メインプロセス（main.js）**: `mainWindow` の `webPreferences` に `backgroundThrottling: false` を追加。
- **効果**: メイン画面を閉じてミニ画面のみを表示している（バックグラウンド状態の）場合でも、ChromiumによるReact側のタイマー（`setInterval`）の制限を受けなくなり、疲労度チェックが定期的に行われ、正しく疲労アラートが通知されるように改善。

## 2026-05-05 00:48
### メイン画面の稼働率・集計中表示の配置調整
- **フロントエンド（App.jsx）**: メイン画面のダッシュボード（疲労ゲージカード）において、「稼働率 集計中. . .」や稼働率の文字列が「Calculating」などのステータス名と横並びになっていたのを、ステータス名の下に移動して `flexDirection: 'column'` による縦並び配置へと変更しました。

## 2026-05-05 00:55
### 疲労度計算におけるスライディングウィンドウの改善
- **バックエンド（server.js）**: 起動直後や30分経過直後の急な「Critical」判定を防ぐため、スライディングウィンドウ算出時の期待ログ数（分母）を常に60分（3600秒）固定として計算するよう改善。
- **効果**: アプリ起動前の時間帯を「休憩時間」として適切に評価できるようになり、不当なCritical判定を防げるようになりました。

## 2026-05-05 01:05
### Calculating（集計中）猶予期間の完全撤廃
- **バックエンド（server.js）**: スライディングウィンドウの分母固定化により起動直後の「Critical」暴発問題が解消されたため、稼働開始から最初の30分間を強制的に判定保留とする「Calculating」ステータスの猶予期間ロジックを完全に削除。
- **フロントエンド（App.jsx）**: 「Calculating」に依存するペット描画ロジック、アドバイステキスト表示、ゲージ幅（0%固定化）などの各種UI処理を一掃し、コードのクリーンアップを実施。
- **仕様書（specification.md）**: 30分の猶予期間（Calculating）に関する記述を削除し、最新のステータス遷移仕様（5段階＋Flesh）に沿った内容に更新。
- **効果**: 起動直後からリアルタイムに疲労度（Restored等）が確認できるようになり、即時性とユーザー体験が向上。
## 2026-05-05 01:15
### 初回ログ取得前（10秒間）の初期状態設定
- **バックエンド（server.js）**: データが1件もない（起動直後やログリセット直後の最初の10秒間）状態のステータスを `Flesh` から `Initializing` に変更。
- **フロントエンド（App.jsx）**: ステータスが `Initializing` の間は、ペットアイコンを非表示（`visibility: hidden`）にし、ステータス名表示を「Initializing . . .」、稼働率表示を「集計中. . .」とするようUI描画ロジックを修正。ゲージ幅も0%となるよう適用。
- **効果**: 起動後最初のログが取得されるまでのわずかな時間（約10秒間）も、不自然に「Restored」や元気なペットが表示されることなく、「Initializing . . .」として適切に待機状態を示すようになりました。
## 2026-05-05 01:42
### ナビゲーションメニューのタブ順変更
- **フロントエンド（App.jsx）**: サイドバー（ナビゲーション）において、「履歴」と「タイムテーブル」の並び順を入れ替え、タイムテーブルが先に表示されるように変更しました。
## 2026-05-05 01:48
### タイムテーブルのアイコン変更
- **フロントエンド（App.jsx）**: 「ダッシュボード」とアイコンが混同しやすかったため、「タイムテーブル」のアイコンを `LayoutGrid` から 3×3のより細かなセルを連想させる `Grid` へと変更しました。
## 2026-05-05 01:50
### タイムテーブル内のタイトルアイコン参照の修正
- **フロントエンド（App.jsx）**: タイムテーブル画面内でのタイトル部分にあるアイコンも `Grid` へと置き換えることで、定義エラーによる画面クラッシュを解消しました。
## 2026-05-05 01:55
### 環境設定・ヘルプ画面の日付範囲指定の非表示化
- **フロントエンド（App.jsx）**: 「環境設定」と「ヘルプ・ガイド」タブのヘッダーにおいて、日付の絞り込み（期間指定）を行う必要がないため非表示に修正しました。

## 2026-05-05 11:38
### Strained/Criticalペットの左右非対称化と反転移動の実装
- **フロントエンド（App.jsx）**: `Strained` と `Critical` 状態のペットの目と口を左側に寄せ、左右非対称なデザインに変更（デフォルトで左を向いているように見えるように調整）。
- **フロントエンド（App.css）**: 
  - `pet-shake` および `pet-danger-shake` アニメーションを刷新。
  - 進行方向（左移動時は `scaleX(1)`、右移動時は `scaleX(-1)`）に合わせてペットを反転させるロジックを追加。
  - 移動距離を大きく（15px〜18px）し、あちこち動き回る「右往左往」している感じを強化。
  - アニメーションが中心から正しく反転するように `transform-origin: center` を追加。
- **効果**: ペットが焦って右往左往している様子がよりリアルに表現されるようになりました。





## 2026-05-05 11:42
### ペットアニメーションの往復ループ化
- **フロントエンド（App.css）**: 往復アニメーションの不自然な反転（中心位置での向き反転）を解消。左端から右端への一方向移動でアニメーションを構成し、往復設定（`alternate`）を使用することで、端に到達した時のみ向きが反転して戻る自然な挙動に修正しました。

## 2026-05-05 12:28
### リアルタイムステータス表示の2点化とPC稼働状態の反映
- **バックエンド（server.js / collector.js）**:
  - PC自体の操作状態（Idle/Active）をリアルタイムに管理するためのエンドポイント `/api/status` を追加。
  - コレクターがサンプリングごとに、PCの操作状態をサーバーへ通知する仕組みを構築。
- **フロントエンド（App.jsx）**:
  - インジケーターを2つのドットに変更。
    - **左ドット**: アプリの記録状態（緑：記録中 / 灰：停止中）
    - **右ドット**: PCの操作状態（青：操作中 / 灰：無操作）
  - 点滅（パルス）アニメーションを廃止し、静的な表示に変更。
  - 10秒ごとのポーリングにPCステータスの取得を追加。
- **フロントエンド（App.jsx）**:
  - ステータスインジケーター（2つのドット）の配置を、ユーザーの要望に合わせて最適化しました。
    - **ミニ画面**: ヘッダーから削除し、情報の邪魔になりにくい**右下（稼働時間の横）**へ移動。
    - **メイン画面**: サイドバーから削除し、ダッシュボードの**「疲労ゲージ」カードの右上**へ移動。
  - 【バグ修正】カードタイトルのレイアウト修正時に発生した JSX の閉じタグ不整合を修正し、画面が正常に表示されるように復旧しました。
  - 無操作時にドットが即座に非表示になる仕様は維持しています。
- **効果**: 各画面において、より自然で視線の邪魔にならない位置にステータスが表示されるようになり、UIの完成度が向上しました。

## 2026-05-05 11:42
### ペットアニメーションの往復ループ化
- **フロントエンド（App.css）**: 往復アニメーションの不自然な反転（中心位置での向き反転）を解消。左端から右端への一方向移動でアニメーションを構成し、往復設定（`alternate`）を使用することで、端に到達した時のみ向きが反転して戻る自然な挙動に修正しました。
- **効果**: 中心点での一時停止がなくなり、スムーズな移動を実現。

## 2026-05-05 11:47
### プレビューボタンの削除
- **フロントエンド（App.jsx）**: 確認用に一時設置していたプレビューボタンと `previewStatus` ステートを削除。コードを元のクリーンな状態に復元しました。

## 2026-05-05 15:55
### スライディングウィンドウと無操作判定時間の変更
- **バックエンド（collector.js）**: 無操作判定しきい値（`idleThreshold`）を120秒から60秒（1分）に変更。
- **バックエンド（server.js）**: 
  - DB初期化時の `idle_threshold` デフォルト値を60秒に更新。
  - 疲労度計算のスライディングウィンドウを60分から90分に延長。
  - 期待ログ数算出の分母を5400秒（90分）に修正。
- **フロントエンド（App.jsx）**:
  - ステータスインジケーター（ドット）の表示条件を `idleSeconds < 60` に変更。
  - ヘルプ画面内の「60分スライディングウィンドウ方式」を「90分スライディングウィンドウ方式」に更新。
  - ヘルプ画面内の「120秒間（2分）操作がない場合」を「60秒間（1分）操作がない場合」に更新。
- **効果**: より長期間（90分）の活動傾向に基づいた疲労度算出と、より迅速な（1分）無操作判定を両立。

### 実行ファイルのビルド
- 2026-05-05 16:41: 最新の設定（90分ウィンドウ/60秒判定）を反映したEXEファイルの再ビルドを実施。
- `npm run build` を実行し、`dist-pulse` フォルダ内の `PulseWork 1.0.0.exe` を更新。

## 2026-05-07 16:20
### ポモドーロモードの実装
- **バックエンド**: 
  - `settings` テーブルに `current_mode` と `pomodoro_start_ms` を追加。
  - `/api/fatigue` エンドポイントを拡張し、ポモドーロモード（15分/3分、25分/5分、50分/10分）のフェーズ判定（Work/Break）と残り時間の計算ロジックを実装。
- **フロントエンド**:
  - 稼働モード切り替え機能（トラッキング / ポモドーロ15 / ポモドーロ25 / ポモドーロ50）を実装。
  - ダッシュボードの疲労ゲージカードにモード選択UIとポモドーロタイマー表示を追加。
  - ミニ画面（ウィジェット）をポモドーロ表示に対応。作業/休憩フェーズに応じたカラーとタイマーを表示。
  - ポモドーロのフェーズ切り替え時にデスクトップ通知を表示する機能を実装。
  - 設定画面に稼働モード選択セクションを追加。
  - `getFatigueAdvice` を拡張し、ポモドーロフェーズに応じたアドバイスを表示するように修正。
- **効果**: 従来の稼働率ベースの休憩提案に加え、ポモドーロ・テクニックを用いた規則的な作業・休憩リズムの管理が可能になりました。
- **修正**: 実装時に発生した `App.jsx` 内の構文エラー（`div` タグの閉じ忘れ・開き忘れ）を修正。
- **改善**: ポモドーロモード中、疲労ゲージの代わりに現在のフェーズ（作業/休憩）の進捗状況を示すプログレスバーを表示するように変更。
- **改善**: ポモドーロのフェーズ切り替え時のアラートを、疲労アラートと同様の最前面ポップアップダイアログ形式に変更。
- **最適化**: ポモドーロモード中は稼働率の計算（DBクエリ）をスキップし、リソース消費を抑制するように変更。稼働率とステータス判定はトラッキングモード選択時のみ実行。
- **修正**: ポモドーロのフェーズ切り替えアラートがメイン画面とミニ画面の両方で発火し、二重に表示される問題を修正（ミニ画面での発火を抑制）。
- **改善**: ポモドーロの残り時間表示を、10秒ごとの同期からフロントエンドでの1秒刻みのカウントダウン方式に変更し、よりスムーズなタイマー表示を実現。
- **新機能**: ポモドーロモードに「再開」「一時停止」「リセット」ボタンを追加。状況に合わせて手動でタイマーをコントロールできるように拡張。
- **改善**: ダッシュボードのレイアウトを調整。1行目にステータスとペット、2行目に残り時間と操作ボタンを配置。ボタンを右端に固定することで、カウントダウンの数字の変化によるボタンのガタつきを解消。
- **UI調整**: モード切り替えプルダウン横の重複していたラベル（トラッキング/ポモドーロ）を削除し、UIをクリーンに整理。
- **改善**: ポモドーロモード切替時にタイマーが自動で開始されないように変更（一時停止状態で開始）。また、開始ボタンのラベルを「再開」から「スタート」に変更し、より直感的に。
- **ドキュメント**: ヘルプ・ガイド画面に「ポモドーロモード」の解説項目と、便利なヒントを追加し、最新の機能を反映。
