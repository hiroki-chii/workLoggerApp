# 作業ログ

## 2026-05-21 11:33
- `/rebuild-overwrite` ワークフローを実行。メイン画面とミニ画面のリアルタイム同期ズレ修正を適用したインストーラー（nsis / portable）の上書き再ビルドを完了。

## 2026-05-21 11:29
- 同期ズレ修正の変更内容を GitHub へコミット＆プッシュ（コミットメッセージ: 「メイン画面とミニ画面（ウィジェット）間の即時同期機能の実装（IPCイベント駆動による状態同期）」）。

## 2026-05-21 11:26
- メイン画面とミニ画面（ウィジェット）間の表示同期ズレを修正。
  - `electron-core/main.js` に `window-event:notify` IPCイベントを受信してアクティブな全ウィンドウ（メイン・ミニ）へブロードキャストする仕組みを追加。
  - `frontend/src/utils/helpers.js` に `sendIpc` ヘルパー関数を追加。
  - `frontend/src/App.jsx` で、ポモドーロ制御、記録トグル、設定保存、ウィンドウルール変更、ログリセットなどの状態変更を伴うアクション実行時に同期通知を送信。また、各ウィンドウがその通知を受け取った瞬間に `fetchData()` を呼び出すイベント駆動型の同期を実装し、ポーリングによる最大10秒の表示遅延を解消。

## 2026-05-21 10:37
- `/rebuild-overwrite` ワークフローを実行。既存の `dist-pulse` ディレクトリのファイルを一旦クリアし、`npm run build` を実行してインストーラー（nsis / portable）のクリーン＆上書きビルドを完了。

## 2026-05-21 10:33
- 新バージョンとしてバージョンを上げてからビルド（パッケージング）を行うためのワークフロー/プロンプト案を作成し、ユーザーに提案。

## 2026-05-21 10:30
- `rebuild.md` ワークフローの修正指示に対し、セキュリティ保護により `.gemini` ディレクトリ以下の直接編集が制限されているため、元の成果物に上書きする形で再ビルドを行う記述への修正案をユーザーに提示。

## 2026-05-21 10:14
- 前回のミニ画面修正内容を GitHub へコミット＆プッシュ（コミットメッセージ: 「ミニ画面が表示されなくなる不具合の修正（画面外座標の自動フォールバックおよびパッケージ環境でのロード処理改善）」）。

## 2026-05-21 10:05
- ミニ画面が表示されなくなる不具合を修正。
  - `electron-core/main.js` の `createMiniWindow` 内で、データベースから読み込んだ座標 `(x, y)` が、マルチモニター切断などにより画面外にはみ出ていないかを検証するガードロジックを追加。画面外の場合は初期位置（プライマリ画面の右下）へ自動フォールバックするよう修正。
  - 本番パッケージ環境において、`file://` プロトコル経由でクエリパラメータ（`?mini=true`）付きのローカルファイルを確実に読み込むため、`loadURL` の代わりに Electron 推奨の `loadFile` メソッドを使用し、`query` オプションでパラメータを渡すように修正。
  - `createWindow` においても同様に `loadFile` を使用するよう統一。

## 2026-05-18 20:05
- 「作業履歴」内の表示件数上限を100件から1000件に増加。
- 「作業履歴」タブにおける期間設定UIを廃止し、常に最新の履歴を表示するように変更。
- `backend/server.js` および `frontend/src/App.jsx` を更新。
- 最新の修正を反映した exe ファイル（`dist-pulse/ゆとリズム Setup 1.0.0.exe`）の再ビルドを完了。

## 2026-05-18 19:50
- CSV出力（履歴CSV、タイムテーブルCSV）の際に、表示名変更ルール（`window_rules`）が適用されているウィンドウタイトルを変更後の表示名で出力するように修正。
- `backend/server.js` を更新。

## 2026-05-16 11:47
- エクスポートモーダルから「すべての履歴を出力」「すべてのタイムテーブルを出力」の項目を削除。
- 期間指定によるエクスポートに一本化し、UIを簡素化。
- `App.jsx` を更新。

## 2026-05-16 11:40
- タイムテーブルと同じグリッド形式でCSV出力できる機能を追加。
- バックエンドに `/api/export/timetable` エンドポイントを新設。
- フロントエンドのエクスポートモーダルに「タイムテーブルCSV」ボタンを追加し、期間指定出力と全期間出力の両方に対応。
- `server.js` および `App.jsx` を更新。

## 2026-05-14 13:51
- メイン画面左上のロゴとアプリ名（ゆとリズム）を左寄せに変更。
- サイドバーが折りたたまれている時は、トグルボタンが中央に配置されるように調整。
- GitHub へのプッシュを完了（コミットメッセージ: 「メイン画面左上のロゴとアプリ名を左寄せに変更」）。
- 最新の修正を反映した exe ファイル（`dist-pulse/ゆとリズム Setup 1.0.0.exe`）の再ビルドを完了。




## 2026-05-14 10:00
- サーバー接続エラー時のメッセージを、技術的な内容からユーザーに待機を促す柔らかい表現（「サーバーを準備しています。そのまま少々お待ちください...」）に変更。
- `specification.md` を最新のアプリ名（ゆとリズム）、デザインテーマ、スライディングウィンドウ設定機能に合わせて全面的に更新。
- `README.md` を最新のアプリ名（ゆとリズム）、デザインコンセプト、新機能（スライディングウィンドウ設定）に合わせて更新。
- GitHub へのプッシュを完了（コミットメッセージ: 「サーバー接続エラーメッセージの改善、および仕様書・READMEの最新化」）。
- 最新の修正内容を反映したexeファイルを再ビルド（`dist-pulse/ゆとリズム Setup 1.0.0.exe`）を完了。

## 2026-05-13 00:30
- アプリ名を「WorkPulse」から「ゆとリズム」に変更。
- デザインテーマを「ゆとり」と「リズム」をテーマにしたバイオレット＆ティール系に刷新。
- ロゴアイコンを新しく提供されたオリジナルのSVGデザイン（円形背景、リズム波線、ドットアクセント）に刷新。
- 「CSV出力」ボタンの色を、メインテーマに馴染むアクセントカラー（ティール系）に変更。
- アプリのアイコン画像（`favicon.svg` および `assets/icon.png`）を新しいロゴデザインに更新。
- `package.json` の `productName` を「ゆとリズム」に変更し、ビルド後のファイル名を統一。
- exeファイルを再ビルド（`dist-pulse/ゆとリズム Setup 1.0.0.exe`）を完了。
- `App.jsx`, `App.css`, `index.html`, `server.js` を更新。
- ハードコードされていた色設定を可能な限りCSS変数（`var(--primary)`等）に置き換え、メンテナンス性を向上。
